# Ralph Progress Log
Started: Tue Jan 27 18:12:54 PST 2026

## Codebase Patterns
- SQL migrations go in supabase/migrations/ with format NNN_description.sql
- Use `IF NOT EXISTS` for all CREATE TABLE and CREATE INDEX statements
- RLS policies follow naming pattern: `tablename_action_description`
- Document all migrations in SUPABASE_MIGRATIONS.md under "Completed Migrations"
- auth.uid() is used for RLS policies to reference current authenticated user
- Edge Functions go in supabase/functions/<function-name>/index.ts
- Shared Edge Function utilities in supabase/functions/_shared/
- Use `https://esm.sh/@supabase/supabase-js@2` for Deno imports
- Edge Functions access env vars via Deno.env.get('VARIABLE_NAME')
- Use shared idempotency helper (checkIdempotency, storeIdempotency) from _shared/idempotency.ts for all mutation Edge Functions
- Use shared audit helper (emitAuditEvent) from _shared/audit.ts to emit audit events after state changes

---

## 2026-01-27 - US-001
- **What was implemented**: User agreements database schema for storing legal acknowledgments
- **Files changed**:
  - Created: supabase/migrations/003_user_agreements.sql
  - Updated: SUPABASE_MIGRATIONS.md
- **Learnings for future iterations:**
  - The user_agreements table uses auth.users(id) reference (not bookies/players tables) since it tracks all users
  - RLS policies are intentionally missing UPDATE and DELETE to enforce immutability
  - The role CHECK constraint only allows 'bookie' or 'player' values
---

## 2026-01-27 - US-002
- **What was implemented**: UserAgreement SwiftData model for caching agreement status locally
- **Files changed**:
  - Created: Booki/Models/UserAgreement.swift
  - Updated: Booki/BookiApp.swift (added UserAgreement.self to Schema)
- **Learnings for future iterations:**
  - SwiftData models need @Attribute(.unique) for primary key UUID fields
  - UserAgreement is a simple cache model - server (user_agreements table) is the source of truth
  - SourceKit errors about SwiftDataMacros are expected when checking files outside Xcode - syntax validation with `swiftc -parse` is sufficient
  - Per CLAUDE.md: "Xcode command line tools don't support full builds - use Xcode IDE"
---

## 2026-01-27 - US-003
- **What was implemented**: AgreementService for checking and submitting user agreement acceptances to Supabase
- **Files changed**:
  - Created: Booki/Services/AgreementService.swift
- **Learnings for future iterations:**
  - Services that query Supabase use `SupabaseClientManager.shared.client` singleton
  - Use Codable structs with CodingKeys for snake_case to camelCase mapping (e.g., `user_id` -> `userId`)
  - AgreementStatus enum has three states: accepted, required, outdated
  - currentAgreementVersion is "1.0" - increment when terms change to require re-acceptance
  - UserAgreementRecord/UserAgreementInsert are private structs for Supabase serialization
---

## 2026-01-27 - US-004
- **What was implemented**: UserAgreementView UI for displaying and accepting Terms of Service
- **Files changed**:
  - Created: Booki/Views/UserAgreementView.swift
- **Learnings for future iterations:**
  - Views follow pattern: ZStack with Theme.backgroundGradient ignoresSafeArea, then ScrollView with content
  - Use Theme colors consistently: Theme.accent, Theme.textPrimary, Theme.textSecondary, Theme.cardBackground, Theme.cornerRadius, Theme.cornerRadiusSmall
  - Checkbox-style toggle implemented as a Button with Image(systemName: "checkmark.square.fill"/"square")
  - Sheet presentation uses NavigationStack with toolbar for Done button
  - Added optional `message` parameter to UserAgreementView for displaying update notices (used in US-008)
  - Continue button uses disabled state with opacity changes (hasAgreed ? Theme.accent : Theme.accent.opacity(0.5))
---

## 2026-01-27 - US-005
- **What was implemented**: Terms of Service content as static strings in a dedicated file
- **Files changed**:
  - Created: Booki/Views/TermsOfService.swift
  - Updated: Booki/Views/UserAgreementView.swift (now uses TermsOfService.summary and TermsOfService.fullTerms)
- **Learnings for future iterations:**
  - TermsOfService is an enum with static let properties (not a struct) since it only contains constants
  - Summary text is displayed on the main agreement screen, fullTerms shown in the sheet
  - TermsOfService.version constant added ("1.0") for reference - should match AgreementService.currentAgreementVersion
  - The fullTerms includes string interpolation to embed the version: `Version: \(version)`
---

## 2026-01-27 - US-006
- **What was implemented**: Agreement integration into bookie signup/login flow
- **Files changed**:
  - Updated: Booki/Services/AuthManager.swift (added agreementRequired property, agreement methods)
  - Updated: Booki/Views/AuthGateView.swift (added bookieAgreementView and conditional routing)
- **Learnings for future iterations:**
  - AuthManager owns the agreementRequired state and agreement operations (check/submit)
  - AgreementService instance is held by AuthManager, not created per call
  - Agreement check happens in ensureBookieRecord() after bookie record is established
  - AuthGateView conditionally shows UserAgreementView when agreementRequired && userRole == .bookie
  - The bookie's UUID (currentBookieId) is used for agreement submissions, not the auth user ID
  - agreementRequired is reset to false in updateAuthState when user signs out
---

## 2026-01-27 - US-007
- **What was implemented**: Agreement integration into player claim flow
- **Files changed**:
  - Updated: Booki/Views/PlayerClaimView.swift (added agreement state, showAgreement logic, submitPlayerAgreement function)
- **Learnings for future iterations:**
  - PlayerClaimView now shows UserAgreementView after account creation, before showing success screen
  - The flow is: validate code → create account → show agreement → submit agreement → show success → navigate to login
  - createdAuthUserId stores the auth user ID from signup response for agreement submission
  - If agreement submission fails, the player still proceeds to success screen - agreement will be required on next login
  - The view conditionally shows UserAgreementView at the top level (outside the ZStack) to avoid nested ScrollViews
  - AgreementService is instantiated locally in submitPlayerAgreement() rather than held as a property
---

## 2026-01-27 - US-008
- **What was implemented**: Enforce agreement check on app launch for all authenticated users (bookies and players)
- **Files changed**:
  - Updated: Booki/Services/AuthManager.swift (added agreementIsOutdated property, player agreement check)
  - Updated: Booki/Views/AuthGateView.swift (generalized agreement view for both roles, added update message)
- **Learnings for future iterations:**
  - agreementIsOutdated (Bool) tracks whether it's an updated terms vs never-accepted, to show appropriate message
  - AuthGateView now blocks BOTH bookies and players when agreementRequired is true (was only bookies before)
  - Player agreement check happens in ensureBookieRecord() when user is identified as player (not bookie)
  - setAsPlayer() method now has optional checkAgreement parameter (default true) to trigger agreement check
  - The message "We have updated our Terms of Service. Please review and accept to continue." is shown when agreementIsOutdated is true
  - agreementIsOutdated is reset in: updateAuthState (sign out), submitAgreement (on acceptance), checkAgreementRequired (on error)
---

## 2026-01-27 - US-009
- **What was implemented**: Supabase Edge Functions infrastructure with shared utilities
- **Files changed**:
  - Created: supabase/functions/_shared/cors.ts (CORS headers constant)
  - Created: supabase/functions/_shared/supabase.ts (createServiceClient, createUserClient, getUserIdFromAuthHeader helpers)
  - Created: supabase/functions/README.md (documentation and deployment instructions)
- **Learnings for future iterations:**
  - Edge Functions use Deno runtime with ESM imports from https://esm.sh/
  - CORS headers must be included in all responses for iOS app to communicate with functions
  - createServiceClient() uses SUPABASE_SERVICE_ROLE_KEY to bypass RLS
  - createUserClient() uses the user's JWT token to respect RLS
  - getUserIdFromAuthHeader() extracts user ID from JWT by calling client.auth.getUser()
  - Deno is not installed locally; typecheck happens during Supabase deployment
  - All mutation functions should use idempotency_key pattern to prevent duplicate operations
  - Idempotency keys table stores: key (client-provided), operation (function name), response (cached JSON), user_id, expires_at
  - submit_bet validates: player ownership, event existence, event not locked (status != locked AND start_time > now)
---

## 2026-01-27 - US-010
- **What was implemented**: submit_bet Edge Function for server-authoritative bet submissions
- **Files changed**:
  - Created: supabase/functions/submit_bet/index.ts
  - Created: supabase/migrations/004_idempotency_keys.sql
  - Updated: SUPABASE_MIGRATIONS.md
- **Learnings for future iterations:**
  - Edge Functions use createServiceClient() for operations that need to bypass RLS (like inserting bets on behalf of players)
  - Idempotency check happens BEFORE any validation to return cached response immediately
  - Event locking is determined by: status === 'locked' OR start_time <= now
  - ticket_id is generated server-side with crypto.randomUUID() for grouping bets from the same submission
  - The market_id from request maps to `market` column in bets table (historical naming)
  - Player validation ensures: 1) player exists, 2) belongs to bookie, 3) auth_user_id matches JWT user
---

## 2026-01-27 - US-011
- **What was implemented**: accept_bet Edge Function for server-authoritative bet acceptance by bookies
- **Files changed**:
  - Created: supabase/functions/accept_bet/index.ts
  - Created: supabase/migrations/005_bets_accepted_at.sql (adds accepted_at column to bets table)
  - Updated: SUPABASE_MIGRATIONS.md
- **Learnings for future iterations:**
  - Bookie validation pattern: lookup bookie by auth_user_id from JWT, then verify bet.bookie_id matches bookie.id
  - Status validation should provide specific error messages for each invalid status (accepted, declined, graded, settled)
  - accepted_at column was added to bets table to track acceptance timestamp (was not in original schema)
  - Same patterns as submit_bet: CORS preflight, idempotency check early, createServiceClient() for mutations
---

## 2026-01-27 - US-012
- **What was implemented**: grade_bet Edge Function for server-authoritative bet grading by bookies
- **Files changed**:
  - Created: supabase/functions/grade_bet/index.ts
- **Learnings for future iterations:**
  - Outcome values stored in `grade_result` column (not `outcome`) - matches existing bets table schema
  - Valid outcomes are: win, loss, push, void - validated against VALID_OUTCOMES array
  - Status changes from 'accepted' to 'graded' when outcome is recorded
  - Grading requires bet to be 'accepted' status - pending bets must be accepted first, settled bets cannot be regraded
  - Same pattern as accept_bet: bookie validation via auth_user_id lookup, idempotency check, createServiceClient()
---

## 2026-01-27 - US-014
- **What was implemented**: adjust_balance Edge Function for server-authoritative manual balance adjustments
- **Files changed**:
  - Created: supabase/functions/adjust_balance/index.ts
- **Learnings for future iterations:**
  - Manual adjustments use ledger_entry type 'adjustment' (not 'settlement')
  - Adjustments are NOT tied to bets, so bet_id is null in the ledger entry
  - Amount is passed as Decimal string in request, parsed as float for validation and storage
  - Reason is required and used as the ledger entry description (trimmed for whitespace)
  - Player ownership validation: first lookup bookie by auth_user_id, then verify player.bookie_id matches bookie.id
  - Same pattern as other Edge Functions: CORS preflight, idempotency check early, createServiceClient() for mutations
  - Response includes only the created ledger_entry (no bet since this is a manual adjustment)
---

## 2026-01-27 - US-015
- **What was implemented**: EdgeFunctionService for calling Supabase Edge Functions from iOS app
- **Files changed**:
  - Created: Booki/Services/EdgeFunctionService.swift
- **Learnings for future iterations:**
  - EdgeFunctionService is a singleton (static let shared) matching pattern of other services
  - Uses SupabaseClientManager.shared.client to access the Supabase client
  - Access token obtained via `supabase.auth.session.accessToken`
  - URL constructed as: SupabaseConfig.url + /functions/v1/ + functionName
  - AnyEncodable type-erasure wrapper needed to encode generic Encodable body
  - EdgeFunctionError enum handles: notAuthenticated, invalidURL, networkError, serverError (with message), decodingError, encodingError
  - Uses URLSession.shared.data(for:) for the actual HTTP request
  - JSONDecoder uses .iso8601 date decoding strategy
---

## 2026-01-27 - US-016
- **What was implemented**: Updated iOS bet submission to use submit_bet Edge Function instead of direct Supabase inserts
- **Files changed**:
  - Updated: Booki/Services/BetService.swift (added SubmitBetRequest, SubmitBetResponse structs, submitBetToServer method, createLocalBetFromResponse helper)
  - Updated: Booki/Views/BetSlipSheet.swift (submitBets now uses EdgeFunctionService)
  - Updated: Booki/Views/BetConfirmationSheet.swift (submitBets now uses EdgeFunctionService)
  - Updated: Booki/Views/SubmitBetView.swift (submitRequest now uses EdgeFunctionService)
- **Learnings for future iterations:**
  - BetServiceError extended with .edgeFunctionError(String) case for Edge Function errors
  - Equatable conformance for BetServiceError now uses explicit implementation due to associated String value
  - SubmitBetRequest uses CodingKeys for snake_case conversion (event_id, player_id, etc.)
  - submitBetToServer generates idempotency_key as UUID().uuidString for each submission
  - createLocalBetFromResponse creates local Bet from server response, using localSide and localMarket params to preserve iOS naming
  - Bet submission views now use Task { } for async Edge Function calls with MainActor.run for UI updates
  - player.bookieId is required for Edge Function calls - returns error if nil
  - Server returns bet with needsSync: false since it's already on server
---

## 2026-01-29 - US-017
- **What was implemented**: Shared idempotency helper for Edge Functions to eliminate code duplication
- **Files changed**:
  - Created: supabase/functions/_shared/idempotency.ts (checkIdempotency, storeIdempotency functions)
  - Updated: supabase/functions/submit_bet/index.ts (uses shared idempotency helper)
  - Updated: supabase/functions/accept_bet/index.ts (uses shared idempotency helper)
  - Updated: supabase/functions/grade_bet/index.ts (uses shared idempotency helper)
  - Updated: supabase/functions/settle_bet/index.ts (uses shared idempotency helper)
  - Updated: supabase/functions/adjust_balance/index.ts (uses shared idempotency helper)
- **Learnings for future iterations:**
  - Shared Edge Function helpers go in supabase/functions/_shared/ directory
  - checkIdempotency returns the cached response string if key exists, null otherwise
  - storeIdempotency handles its own error logging (swallows errors since mutation already succeeded)
  - SupabaseClient type is imported from https://esm.sh/@supabase/supabase-js@2
  - Deno is not installed locally - typecheck happens during Supabase deployment
---

## 2026-01-29 - US-018
- **What was implemented**: Retry logic with exponential backoff for iOS Edge Function calls
- **Files changed**:
  - Updated: Booki/Services/EdgeFunctionService.swift (added retryWithBackoff method, updated callFunction to use it)
- **Learnings for future iterations:**
  - URLError is the Swift type for network-level errors (connection lost, timeout, etc.)
  - Exponential backoff formula: `pow(2.0, Double(attempt - 1))` gives 1, 2, 4 for attempts 1, 2, 3
  - Task.sleep uses nanoseconds: multiply seconds by 1_000_000_000
  - The retry logic preserves the same URLRequest (including idempotency key in the body) across retries
  - Server errors (4xx/5xx) are NOT retried - only URLError (network issues)
  - The retryWithBackoff method is generic and could be reused for other network operations if needed
---

## 2026-01-29 - US-019
- **What was implemented**: Audit events database schema for comprehensive state change logging
- **Files changed**:
  - Created: supabase/migrations/006_audit_events.sql
  - Updated: SUPABASE_MIGRATIONS.md
- **Learnings for future iterations:**
  - Audit events use JSONB for previous_state and new_state to store flexible JSON snapshots
  - RLS policy uses subquery pattern: `bookie_id IN (SELECT id FROM bookies WHERE auth_user_id = auth.uid())`
  - No INSERT/UPDATE/DELETE policies for audit_events - Edge Functions use service role to insert
  - Two indexes: composite (bookie_id, entity_type, entity_id) for entity lookups, composite (bookie_id, created_at) for timeline queries
  - actor_user_id uses ON DELETE SET NULL (not CASCADE) to preserve audit history even if user is deleted
---

## 2026-01-29 - US-020
- **What was implemented**: Shared audit helper for emitting audit events from Edge Functions
- **Files changed**:
  - Created: supabase/functions/_shared/audit.ts
- **Learnings for future iterations:**
  - emitAuditEvent accepts a params object with: bookieId, actorUserId, entityType, entityId, actionType, previousState?, newState, reason?
  - EntityType is a union type: 'bet' | 'ledger_entry' | 'player' | 'event'
  - ActionType is a union type: 'create' | 'accept' | 'grade' | 'settle' | 'adjust' | 'reverse' | 'override'
  - Errors are logged but not thrown (matches idempotency pattern) since the mutation already succeeded
  - Uses Record<string, unknown> for JSONB fields to allow flexible JSON objects
  - Import from '_shared/audit.ts' with: `import { emitAuditEvent } from '../_shared/audit.ts';`
---

## 2026-01-29 - US-021
- **What was implemented**: Emit audit events from all existing Edge Functions for traceability
- **Files changed**:
  - Updated: supabase/functions/submit_bet/index.ts (emits 'create' audit for bet)
  - Updated: supabase/functions/accept_bet/index.ts (emits 'accept' audit for bet)
  - Updated: supabase/functions/grade_bet/index.ts (emits 'grade' audit for bet)
  - Updated: supabase/functions/settle_bet/index.ts (emits 'settle' audit for bet, 'create' audit for ledger_entry)
  - Updated: supabase/functions/adjust_balance/index.ts (emits 'adjust' audit for ledger_entry with reason)
- **Learnings for future iterations:**
  - All audit events must include bookieId and actorUserId (from JWT)
  - For bet operations: use bet.bookie_id for bookieId
  - For adjust_balance: use bookie.id (looked up from auth_user_id) for bookieId
  - submit_bet actor is the player (userId), other bet operations actor is the bookie
  - settle_bet emits TWO audit events: one for bet settlement, one for ledger entry creation
  - Use `as unknown as Record<string, unknown>` for type assertion when passing full objects to newState
  - Audit events are emitted AFTER successful mutation but BEFORE idempotency key storage
---

## 2026-01-29 - US-022
- **What was implemented**: reverse_settlement Edge Function to undo bet settlements
- **Files changed**:
  - Created: supabase/functions/reverse_settlement/index.ts
- **Learnings for future iterations:**
  - Reversal creates a new ledger_entry with type 'reversal' and negative of original settlement amount
  - The original settlement ledger_entry is found by querying ledger_entries where bet_id matches and type='settlement'
  - Bet status is changed back from 'settled' to 'graded' (preserving grade_result)
  - Reason is required and non-empty for reversals - used in audit event and ledger entry description
  - Emits TWO audit events: one for bet reversal (action_type='reverse'), one for reversal ledger entry creation
  - Same patterns as other Edge Functions: CORS preflight, idempotency check early, createServiceClient() for mutations
---

## 2026-01-29 - US-023
- **What was implemented**: override_grade Edge Function to change a bet's grade when disputes arise
- **Files changed**:
  - Created: supabase/functions/override_grade/index.ts
- **Learnings for future iterations:**
  - override_grade handles both graded and settled bets - if settled, it first reverses the settlement internally
  - When reversing settlement internally, emits TWO audit events (reverse for bet, create for reversal ledger entry) before the override audit event
  - Returns `settlement_reversed: boolean` to indicate whether a settlement was undone
  - Validates that bet has been graded (grade_result is not null) before allowing override
  - new_outcome field (not outcome) distinguishes this from grade_bet which uses just outcome
  - Same patterns: CORS preflight, idempotency check, bookie ownership validation, audit event emission
---

## 2026-01-29 - US-024
- **What was implemented**: AuditEvent SwiftData model for caching audit history locally
- **Files changed**:
  - Created: Booki/Models/AuditEvent.swift
  - Updated: Booki/BookiApp.swift (added AuditEvent.self to Schema)
- **Learnings for future iterations:**
  - AuditEvent follows same pattern as UserAgreement: @Model class with @Attribute(.unique) for UUID primary key
  - previousState and newState are String types to hold JSON - parsed as needed by consuming code
  - SourceKit errors about SwiftDataMacros are expected outside Xcode - syntax validation with `swiftc -parse` is sufficient
---

## 2026-01-29 - US-025
- **What was implemented**: AuditService for fetching audit history from Supabase
- **Files changed**:
  - Created: Booki/Services/AuditService.swift
- **Learnings for future iterations:**
  - AuditService follows same pattern as AgreementService: private SupabaseClient property, init() calls SupabaseClientManager.shared.client
  - Use Codable structs with CodingKeys for snake_case to camelCase mapping (bookie_id -> bookieId)
  - Private AuditEventRecord struct handles Supabase deserialization, toAuditEvent() method converts to model
  - JSONB columns (previous_state, new_state) come back as String after Codable decoding - no JSON parsing needed
  - Order by created_at descending for most recent first
---

## 2026-01-29 - US-026
- **What was implemented**: BetHistoryView for displaying bet audit history in a timeline format
- **Files changed**:
  - Created: Booki/Views/BetHistoryView.swift
- **Learnings for future iterations:**
  - Views follow pattern: ZStack with Theme.background (not Theme.backgroundGradient for simpler views), then conditional content
  - Loading state: ProgressView with CircularProgressViewStyle(tint: Theme.accent) and "Loading..." text
  - Empty state: VStack with SF Symbol icon, headline title, and subheadline description
  - Error state: Similar to empty with danger color and "Try Again" button
  - Timeline pattern: LazyVStack with enumerated ForEach, using isFirst/isLast flags for line rendering
  - Human-readable action mapping: switch on actionType string to return user-friendly labels (e.g., "create" -> "Bet Created")
  - Action color mapping: different Theme colors for each action type to visually distinguish timeline events
---

## 2026-01-29 - US-027
- **What was implemented**: View History button added to BetDetailView for bookies to access bet audit history
- **Files changed**:
  - Updated: Booki/Views/BetsListView.swift (added NavigationLink to BetHistoryView in BetDetailView)
- **Learnings for future iterations:**
  - BetDetailView is the bookie-facing bet detail view (defined in BetsListView.swift)
  - TicketDetailView is the player-facing ticket detail view (used by TrackView for players)
  - For bookie-only features, add them to BetDetailView since it's only accessible from BetsListView (bookie view)
  - NavigationLink pattern: destination closure first, then label closure with HStack for icon + text + chevron
  - Use clock.arrow.circlepath SF Symbol for history-related actions
---

## 2026-01-29 - US-028
- **What was implemented**: Override Grade button and sheet in BetDetailView for bookies to change bet grades
- **Files changed**:
  - Updated: Booki/Views/BetsListView.swift (added override grade UI, state, sheet, and Edge Function call)
- **Learnings for future iterations:**
  - Override grade includes "void" as an option (win, loss, push, void) unlike regular grading
  - Sheet pattern: NavigationStack with ZStack (Theme.background.ignoresSafeArea) then VStack with content
  - For picker with strings, use ForEach with id: \.self and .tag(value) for binding
  - TextField with axis: .vertical allows multi-line input
  - EdgeFunctionService.callFunction returns decoded response; use private Request/Response structs with CodingKeys for snake_case
  - Async call pattern: Task { await method() } inside button action, MainActor.run for UI updates
  - Warning color (Theme.warning) used for override button tint to indicate caution
  - Button disabled state: combine multiple conditions with || in computed property
---

## 2026-01-29 - US-029
- **What was implemented**: Reverse Settlement sheet in BetDetailView for bookies to undo settled bets
- **Files changed**:
  - Updated: Booki/Views/BetsListView.swift (replaced confirmation dialog with full sheet, added Edge Function call)
- **Learnings for future iterations:**
  - Reverse settlement replaced simple confirmation dialog with full sheet for required reason field
  - Sheet pattern same as override grade: NavigationStack with ZStack (Theme.background.ignoresSafeArea) then VStack
  - Use Theme.danger color for destructive confirmation buttons (reversal) vs Theme.accent for normal actions
  - Success alert shown after sheet dismissal to confirm action completed
  - ReverseSettlementResponse includes both updated bet and the reversal ledger entry
  - reversalImpactDescription computed property reused from existing code to show balance impact in sheet
---
