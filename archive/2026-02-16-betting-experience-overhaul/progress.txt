# Ralph Progress Log
Started: Fri Feb  6 22:53:36 PST 2026

## Codebase Patterns
- Edge Functions use Deno runtime - `tsc` reports false errors for Deno imports/globals; the baseline errors (`.ts` extension imports, `Deno` not found, `req` implicit any) are expected and shared by all Edge Functions
- Always normalize UUIDs to lowercase with `.toLowerCase()` before DB operations (iOS sends uppercase, PostgreSQL stores lowercase)
- Use `Array.from(new Set(...))` instead of spread `[...new Set(...)]` for Set iteration in Edge Functions to avoid downlevelIteration errors
- Audit ActionType must include all action types used across Edge Functions: `bet_submitted`, `bet_auto_accepted`, `bet_auto_graded`
- Events can be shared (bookie_id = NULL) or bookie-specific; validate with `event.bookie_id !== null && event.bookie_id !== requestBookieId`
- All Edge Functions follow the pattern: CORS -> JWT auth -> validate input -> idempotency check -> business logic -> audit event -> store idempotency -> return response
- Swift typecheck: `swiftc -typecheck <file>` shows cross-file errors (types from other files not found) — only errors referencing YOUR new types matter
- No full Xcode available — `xcodebuild` fails, rely on `swiftc -typecheck` for syntax validation
- Swift Edge Function types use snake_case CodingKeys to match server JSON; response types reuse existing structs where the shape matches (e.g., SubmitBetResponseBet)

---

## 2026-02-06 - US-001
- What was implemented: submit_parlay Edge Function (already existed, fixed typecheck issues)
- Files changed:
  - `supabase/functions/submit_parlay/index.ts` - Fixed Set iteration (Array.from instead of spread), added type annotation for event map callback
  - `supabase/functions/_shared/audit.ts` - Added missing ActionType values: `bet_submitted`, `bet_auto_accepted`, `bet_auto_graded`
- **Learnings for future iterations:**
  - The submit_parlay function was already created but had typecheck issues specific to it (Set spread, implicit any, missing ActionTypes)
  - The audit.ts ActionType union was missing several action types actually used by Edge Functions
  - Edge Functions share a baseline of Deno-related tsc errors that are NOT actual bugs (they pass with `deno check`)
---

## 2026-02-06 - US-002
- What was implemented: Added ParlayLeg, SubmitParlayRequest, SubmitParlayResponse, and SubmitParlayDebug Swift types to BetService.swift
- Files changed:
  - `Booki/Services/BetService.swift` - Added 4 new structs with snake_case CodingKeys matching the submit_parlay Edge Function interface
- **Learnings for future iterations:**
  - `swiftc -typecheck` on a single file shows cross-file dependency errors (BetStatus, Player, Bet not found) — these are expected and not real issues
  - `xcodebuild` is not available in this environment (no full Xcode, only CLI tools) — typecheck must rely on `swiftc` single-file checks
  - The Edge Function's `combined_odds` is a number (integer), so Swift type uses `Int` to match
  - SubmitParlayResponse reuses `SubmitBetResponseBet` for the bets array since the server returns the same bet record shape
  - The Edge Function returns a `debug` object on errors with message/details/hint/code fields — added SubmitParlayDebug to capture this
---

## 2026-02-06 - US-003
- What was implemented: Added `submitParlayToServer()` async function to BetService
- Files changed:
  - `Booki/Services/BetService.swift` - Added static function that generates idempotency key, builds SubmitParlayRequest, calls submit_parlay Edge Function via EdgeFunctionService, and returns Result<SubmitParlayResponse, Error>
- **Learnings for future iterations:**
  - Follow the same pattern as `submitBetToServer()`: generate idempotency key, build request, call EdgeFunctionService.shared.callFunction, handle success/failure
  - For parlay errors, append debug info (message/details/hint/code) to the error message for better debugging — the `submitBetToServer` doesn't do this but parlay responses include a debug object
  - Stake is converted to String with `"\(stake)"` to match the server's expected format (same as singles)
---

## 2026-02-06 - US-004
- What was implemented: Updated BetSlipSheet.submitBets() to use parlay endpoint for parlay mode, added createLocalBetsFromParlayResponse helper to BetService
- Files changed:
  - `Booki/Views/BetSlipSheet.swift` - Refactored submitBets() to branch on betMode: parlay mode calls submitParlayToServer() with all items as legs (single network call), singles mode keeps existing per-bet loop
  - `Booki/Services/BetService.swift` - Added createLocalBetsFromParlayResponse() that creates Bet models from SubmitParlayResponse with isParlay=true, parlayLegs=N, and shared ticketId
- **Learnings for future iterations:**
  - BetSlipManager.combinedParlayOdds computes the combined American odds from all items — capture it before async submission
  - When matching server bet responses to local BetSlipItems, match on eventId (lowercased) + sideIndicator since those are unique within a parlay
  - The parlay response returns a single ticketId shared by all legs — all local Bet objects use the same UUID parsed from the response
  - Singles mode uses itemStakes dictionary (per-item stakes), parlay mode uses a single sharedStake — captured before the Task block
---

## 2026-02-06 - US-005
- What was implemented: Fixed per-item stakes initialization when bet slip auto-switches from parlay to singles mode due to conflicting selections
- Files changed:
  - `Booki/Services/BetSlipManager.swift` - Added `modeSwitchMessage` published property, added `initializeItemStakesFromParlay()` to distribute parlay stake evenly to per-item stakes, updated `add()` to call initialization and set message on auto-switch
  - `Booki/Views/BetSlipSheet.swift` - Added inline dismissible banner showing mode switch reason (with accent styling), added `.onChange(of: betMode)` to sync `itemStakeTexts` from manager's pre-populated itemStakes
- **Learnings for future iterations:**
  - BetSlipSheet uses `@State itemStakeTexts` (display strings) separate from BetSlipManager's `itemStakes` (Decimal values) — when programmatically populating stakes, both must be updated
  - The `.onChange(of: betSlipManager.betMode)` modifier syncs text fields from manager stakes — only fills empty text fields to avoid overwriting user input
  - `initializeItemStakesFromParlay()` uses integer division (`NSDecimalNumber(...).intValue`) to get clean per-item amounts — remainder is dropped
  - The mode switch message is dismissible via an X button and uses `Theme.accent` styling to differentiate from the yellow conflict warning
---

## 2026-02-06 - US-006
- What was implemented: Added `displayName` computed property to MarketType enum and updated BetSlipSheet and BetDetailView to use it
- Files changed:
  - `Booki/Models/Market.swift` - Added `displayName` computed property returning title case ("Spread", "Total", "Moneyline")
  - `Booki/Views/BetSlipSheet.swift` - Simplified `marketTypeLabel` to use `item.marketType.displayName.uppercased()`
  - `Booki/Views/BetsListView.swift` - Updated BetDetailView market display to use `MarketType(rawValue: bet.market)?.displayName ?? bet.market`
- **Learnings for future iterations:**
  - `bet.market` stores the raw string value (e.g., "spread") — use `MarketType(rawValue:)?.displayName` to get title case with fallback
  - BetSlipSheet uses UPPERCASE market labels, BetConfirmationSheet uses title case — `.uppercased()` on displayName keeps them distinct
  - BetDetailView is defined inside BetsListView.swift, not a separate file
---

## 2026-02-06 - US-007
- What was implemented: Added sport league abbreviation (e.g., "NBA") before event names in BetsListView bet rows
- Files changed:
  - `Booki/Views/BetsListView.swift` - Added `sportLeague` optional parameter to BetRowView, added `sportLeague(for:)` helper to BetsListView that looks up `event.league`, updated bet row to display "NBA | Lakers @ Celtics" format with accent-colored league abbreviation
- **Learnings for future iterations:**
  - `event.league` is the short abbreviation (e.g., "NBA", "NFL", "MLB") — use this for compact display, not `event.sport` which is the full word (e.g., "Basketball")
  - BetRowView uses optional parameters with defaults for new properties to avoid breaking other call sites
  - The event lookup pattern (`events.first(where: { $0.id.uuidString.lowercased() == bet.eventId.lowercased() })`) is reused from eventName — consider extracting to a shared helper if more lookups are needed
---

## 2026-02-06 - US-008
- What was implemented: Added `eventDescription: String?` and `sportLeague: String?` optional fields to the Bet model for offline event context display
- Files changed:
  - `Booki/Models/Bet.swift` - Added two new nullable String properties with default nil values; SwiftData auto-migrates nullable fields
  - `Booki/Services/BetService.swift` - Updated `createLocalBetFromResponse` to accept optional `eventDescription` and `sportLeague` parameters; updated `createLocalBetsFromParlayResponse` to accept `events: [Event]` parameter, populates `eventDescription` from matching BetSlipItem and `sportLeague` from Event lookup
  - `Booki/Views/BetSlipSheet.swift` - Updated both call sites (singles and parlay) to pass `eventDescription` from BetSlipItem and `sportLeague` from Event lookup
- **Learnings for future iterations:**
  - BetSlipItem already stores `eventDescription` (e.g., "Lakers vs Celtics") — reuse it when creating local Bet objects
  - `event.league` provides the sport abbreviation (e.g., "NBA") — look up the event by UUID (lowercased) to get the league
  - New nullable fields with defaults don't require any changes to existing Bet() call sites — Swift default parameters handle backwards compatibility
  - The parlay create function now takes an `events: [Event]` parameter (defaulting to empty array) so it can look up league info per leg
---

## 2026-02-06 - US-009
- What was implemented: Added eventDescription fallback to all eventName display functions, and sportLeague fallback in BetsListView
- Files changed:
  - `Booki/Views/BetsListView.swift` - Updated `eventName(for:)` and `sportLeague(for:)` in BetsListView, and `eventName` computed property in BetDetailView to use bet.eventDescription/sportLeague as fallback before showing truncated UUID
  - `Booki/Views/TrackView.swift` - Updated `eventName(for:)` to use bet.eventDescription fallback
  - `Booki/Views/TicketDetailView.swift` - Updated `eventName(for:)` to use bet.eventDescription fallback
- **Learnings for future iterations:**
  - The `eventName(for:)` helper function is duplicated across BetsListView, TrackView, and TicketDetailView — each needs to be updated independently
  - BetDetailView is inside BetsListView.swift and uses a computed property `eventName` instead of a function — different pattern but same fallback logic
  - The fallback chain is: Event lookup (from @Query events) -> bet.eventDescription -> "Event \(uuid.prefix(8))"
  - Also added sportLeague fallback in BetsListView's `sportLeague(for:)` using bet.sportLeague
---

## 2026-02-06 - US-010
- What was implemented: Added `sideIndicator: String?` field to Bet model for storing the server-side indicator ('a' or 'b') used for audit purposes
- Files changed:
  - `Booki/Models/Bet.swift` - Added `sideIndicator: String?` property with nil default; added to init parameter list and body
  - `Booki/Services/BetService.swift` - Added `sideIndicator` parameter to `createLocalBetFromResponse()`, passes through to Bet init; updated `createLocalBetsFromParlayResponse()` to populate from `matchingItem?.sideIndicator`
  - `Booki/Services/SyncModels.swift` - Added `sideIndicator: String?` to `BetUpsert` struct, CodingKey `"side_indicator"`, and population from `bet.sideIndicator` in init
  - `Booki/Views/BetSlipSheet.swift` - Updated singles mode call to `createLocalBetFromResponse` to pass `item.sideIndicator`
- **Learnings for future iterations:**
  - Adding a new nullable field to Bet follows a 4-file pattern: Model (Bet.swift), Service (BetService.swift create helpers), Sync (SyncModels.swift BetUpsert), and View (BetSlipSheet.swift call sites)
  - SwiftData auto-migrates nullable fields with nil defaults — no migration needed
  - BetUpsert CodingKeys use snake_case to match PostgreSQL column naming convention
  - The parlay create function gets sideIndicator from `matchingItem?.sideIndicator` (already matched by eventId + sideIndicator), while singles get it directly from `item.sideIndicator`
---

## 2026-02-06 - US-011
- What was implemented: Added `marketId: UUID?` field to Bet model for referencing the Market record
- Files changed:
  - `Booki/Models/Bet.swift` - Added `marketId: UUID?` property with nil default; added to init parameter list and body
  - `Booki/Services/BetService.swift` - Added `marketId` parameter to `createLocalBetFromResponse()`, passes through to Bet init; updated `createLocalBetsFromParlayResponse()` to populate from `matchingItem?.marketId`
  - `Booki/Services/SyncModels.swift` - Added `marketId: UUID?` to `BetUpsert` struct, CodingKey `"market_id"`, and population from `bet.marketId` in init
  - `Booki/Views/BetSlipSheet.swift` - Updated singles mode call to `createLocalBetFromResponse` to pass `item.marketId`
- **Learnings for future iterations:**
  - This follows the exact same 4-file pattern as US-010 (sideIndicator): Bet.swift model → BetService.swift create helpers → SyncModels.swift BetUpsert → BetSlipSheet.swift call sites
  - For parlay mode, `marketId` comes from `matchingItem?.marketId` (BetSlipItem already has marketId); for singles it comes directly from `item.marketId`
  - No new patterns discovered beyond what US-010 already documented
---

## 2026-02-06 - US-012
- What was implemented: Verified locked event handling already exists in submit_parlay Edge Function (lines 175-190)
- Files changed: None — functionality was already implemented
- **Learnings for future iterations:**
  - The submit_parlay function already checks `event.status === 'locked' || startTime <= now` for each event before inserting
  - Locked events cause the entire parlay to be rejected (all-or-nothing)
  - Error format is `'Events locked: [id1, id2]'` — the client side (US-013) should parse this format
  - Always read the existing code before implementing — features may already exist from prior work
---

## 2026-02-06 - US-013
- What was implemented: Display locked event errors in BetSlipSheet with highlighting and removal button
- Files changed:
  - `Booki/Views/BetSlipSheet.swift` - Added `serverLockedEventIds` state, `parseLockedEventIds()` static parser, `removeLockedEvents()` helper, updated locked events alert with "Remove Locked Events" button, pass `isLocked` to PremiumBetSlipItemCard, added lock banner and danger border to locked cards
- **Learnings for future iterations:**
  - Server error format is `'Events locked: [id1, id2]'` — parse by finding `Events locked: [` prefix and extracting IDs before `]`
  - The locked events alert already existed (US-009 client-side check) — enhanced it with a destructive "Remove Locked Events" button
  - PremiumBetSlipItemCard uses default parameter values (`isLocked: Bool = false`) for backwards compatibility
  - When removing items by index in reverse order, indices stay valid — use `reversed()` to avoid index shifting
  - BetSlipSheet has both client-side lock check (pre-submission) and server-side lock parsing (post-submission failure) — the server check catches races where an event locks between client check and server processing
---

## 2026-02-06 - US-014
- What was implemented: Added parlay-specific loading state to BetSlipSheet with mode-aware submit button text, submission progress tracking for singles, and disabled inputs during submission
- Files changed:
  - `Booki/Views/BetSlipSheet.swift` - Added `singlesSubmissionIndex`/`singlesSubmissionTotal` state, `submitButtonLabel` computed property ("Submitting parlay..." vs "Submitting 1 of N..."), disabled mode toggle/stake fields/remove buttons/close button during submission, dimmed button gradient during submission, passed `isSubmitting` to PremiumBetSlipItemCard
- **Learnings for future iterations:**
  - The submit button already had a ProgressView spinner and "Submitting..." text — this story enhanced it to be mode-specific
  - PremiumBetSlipItemCard accepts `isSubmitting` with default `false` for backwards compatibility
  - Singles submission progress requires pre-counting valid items (those with stake > 0) since zero-stake items are skipped
  - Disabling inputs during submission requires touching: Close button, Clear All button (already disabled), mode toggle, parlay stake field + clear button, per-item stake fields (via PremiumBetSlipItemCard), and remove buttons (via PremiumBetSlipItemCard)
---

## 2026-02-06 - US-015
- What was implemented: Added same-game parlay warning that detects when multiple selections come from the same event
- Files changed:
  - `Booki/Services/BetSlipManager.swift` - Added `sameGameParlayWarning: String?` computed property that groups items by eventId and returns warning text with event name when duplicates detected; only active in parlay mode
  - `Booki/Views/BetSlipSheet.swift` - Added warning banner in the header section (after conflict warning, before mode switch message) using Theme.warning styling with exclamation triangle icon; non-blocking (allows submission)
- **Learnings for future iterations:**
  - The warning only appears in parlay mode (`guard betMode == .parlay`) since same-game selections in singles mode are fine
  - This is distinct from `hasConflictingSelections` which blocks parlay mode entirely (same market, different sides) — same-game parlay warning is advisory only
  - The banner styling matches the existing conflict warning pattern (icon + text, warning color, 0.1 opacity background, rounded rectangle)
  - `eventDescription` from `BetSlipItem` provides the human-readable event name (e.g., "Lakers vs Celtics") for the warning text
---
