# Ralph Progress Log
Started: Wed Feb 19 2026
Branch: ralph/compliance-language
---

## Codebase Patterns
- CRITICAL: Only change user-facing string literals (Text(), Label(), .navigationTitle, .tabItem labels, enum rawValues used as display text, error messages assigned to @State vars)
- NEVER rename Swift types, variables, file names, enum cases, or internal identifiers
- NEVER delete code, functions, views, or files — this is a string-rename-only task
- NEVER rewrite or restructure files — use targeted find-and-replace on string literals only
- Use Theme.swift for all colors and styling
- Approved vocabulary: Organizer (not Bookie), Member (not Player), Pick (not Bet), Multi-Pick (not Parlay), Reconciliation (not Settlement), Activity Balance (not Balance), Open Activity (not Exposure), Performance (not Profit/Loss), Success Rate (not Win Rate), Potential Return (not Payout/To Win), Game Lines (not Odds)
- PlayerMainView tabs are: Games, Track, Settings — no banned terms to rename
- BetsListView.swift contains BetsListView, BetRowView, AND BetDetailView — all in one file
- BetFilter/SettlementFilterOption enum rawValues are user-facing display strings
- Ticket.displayName and Ticket.typeLabel extensions are in TrackView.swift
- All dashboard UI (exposure card, pending bets, flagged players) is in DashboardView.swift sub-views
- OnboardingWelcomeView, OnboardingConfigureView exist in Booki/Views/Onboarding/ — do NOT delete them
- BetConfirmationSheet.swift is separate from BetSlipSheet.swift
- "BetMGM" and "Bookmaker" in SettingsView are third-party brand names — exempt from renaming
- PaymentDirection and MarketCategory enum rawValues are used as UI display strings
- Error messages in @State vars (submissionError, errorMessage) are user-facing
---

