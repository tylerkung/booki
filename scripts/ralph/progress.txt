# Ralph Progress Log
Started: Mon Feb 3 13:45:00 PST 2026
Branch: ralph/games-filtering
---

## Codebase Patterns
- Use Theme.swift for all colors (Theme.accent, Theme.danger, Theme.warning, etc.)
- SwiftData models in Booki/Models/, sync models in Services/SyncModels.swift
- EventStatus enum: 'scheduled', 'live', 'final', 'postponed', 'canceled'
- Event.isLocked(offsetMinutes:) determines if betting is locked
- GamesView is the PLAYER's view for browsing games (takes Player parameter)
- EventsListView is the BOOKIE's view for managing events
- All filtering happens in computed properties (not SwiftData predicates)
---

## 2026-02-03 13:50 - US-004
- What was implemented: Filter player game browsing to show only bettable events
- Files changed:
  - Booki/Views/GamesView.swift
- **Learnings for future iterations:**
  - Added bettableEvents computed property: scheduled + not locked + future start time
  - upcomingEvents now combines bettableEvents + recently finished (48hr) for reference
  - Locked events are hidden, not just disabled (per US-004 requirements)
  - Event.isLocked() already existed and handles lock offset from AcceptancePolicy
  - GamesView already queries acceptancePolicies to get lockOffsetMinutes
---

