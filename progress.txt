# Ralph Progress Log
Started: Mon Feb 23 00:39:19 PST 2026
---

## 2026-02-23 - US-001
- Created `Booki/Models/Invite.swift` — SwiftData `@Model` with all required fields
- Fields: id (UUID), bookieId (UUID), inviteCode (String), email (String?), createdAt (Date), expiresAt (Date), claimedAt (Date?), claimedByPlayerId (UUID?), needsSync (Bool), version (Int)
- Computed properties: `isExpired` (expiresAt < Date()), `isPending` (claimedAt == nil && !isExpired)
- Explicit `init()` with all parameters and sensible defaults
- Added `Invite.self` to ModelContainer schema in BookiApp.swift
- Added file to Xcode project (pbxproj)
- Files changed: Booki/Models/Invite.swift (new), Booki/BookiApp.swift, Booki.xcodeproj/project.pbxproj
- **Learnings for future iterations:**
  - Xcode project uses pbxproj — new Swift files must be added to 4 sections: PBXBuildFile, PBXFileReference, group children, and Sources build phase
  - Model doesn't conform to Syncable yet (needs lastSyncedAt) — will be added in US-005 when sync support is implemented
  - SwiftData schema change requires deleting app from Simulator
---

## 2026-02-23 - US-002
- Created `supabase/migrations/009_invites.sql` — invites table with full schema
- Table columns: id (uuid PK), bookie_id (uuid FK), invite_code (text UNIQUE), email (text), created_at, expires_at, claimed_at, claimed_by_player_id (uuid FK), version (int)
- RLS: bookies can SELECT and INSERT own invites; UPDATE/DELETE via service role only (for edge functions)
- Indexes: invite_code (fast lookups during claim), bookie_id (bookie's invite list)
- Documented migration in SUPABASE_MIGRATIONS.md
- Files changed: supabase/migrations/009_invites.sql (new), SUPABASE_MIGRATIONS.md
- **Learnings for future iterations:**
  - RLS pattern for bookie-owned tables: `bookie_id IN (SELECT id FROM bookies WHERE auth_user_id = auth.uid())`
  - Edge functions that modify records on behalf of users (like claiming) need service role — don't add UPDATE RLS policies for those
  - Migration numbering is sequential: check last file number before creating new one
---

## 2026-02-23 - US-003
- Created `supabase/functions/create_invite/index.ts` — server-authoritative invite creation
- Validates JWT, confirms caller is a bookie (queries bookies table for auth_user_id)
- Generates unique 8-char invite code (charset: A-Z excluding O/I/L + 2-9 excluding 0/1) with collision retry (up to 5 attempts)
- Sets expiry to 24 hours from creation
- Inserts invite into `invites` table with bookie_id from authenticated user
- Returns `{ success, invite_id, invite_code, invite_url: 'booki://invite/{code}', expires_at }`
- Full idempotency support, CORS, audit event emission
- Updated `_shared/audit.ts` — added `'invite'` to EntityType, `'invite_created'` and `'invite_claimed'` to ActionType
- Files changed: supabase/functions/create_invite/index.ts (new), supabase/functions/_shared/audit.ts
- **Learnings for future iterations:**
  - Edge function pattern: validate JWT → check idempotency → validate caller role → business logic → insert → audit → store idempotency → return
  - `crypto.getRandomValues()` available in Deno for secure random bytes
  - Invite code charset excludes ambiguous chars (0/O/1/I/L) for readability
  - Added `invite_claimed` ActionType proactively for US-004
---

## 2026-02-23 - US-004
- Created `supabase/functions/claim_invite/index.ts` — server-authoritative invite claiming
- Request body: `{ invite_code, auth_user_id, idempotency_key? }`
- Validates invite exists, is not expired (`expires_at > now()`), and is not already claimed (`claimed_at IS NULL`)
- Verifies auth user exists via `admin.getUserById()`
- Checks auth user isn't already linked to any bookie: queries players for `auth_user_id` WHERE `claimed_at IS NOT NULL` — returns 409 `already_in_group` if found
- Creates new Player record: name = email prefix, status = 'active', credit_limit = 10000, claimed_at = now()
- Updates invite record: claimed_at = now(), claimed_by_player_id = new player ID
- Auto-confirms email via `admin.updateUserById(auth_user_id, { email_confirm: true })`
- Returns `{ success, player_id, bookie_id, player_name }`
- Full idempotency support (optional key), CORS, audit event emission
- Uses service role client to bypass RLS (same pattern as claim_player)
- Files changed: supabase/functions/claim_invite/index.ts (new)
- **Learnings for future iterations:**
  - claim_invite differs from claim_player: creates a NEW player record instead of updating an existing one
  - The `already_in_group` check prevents users from joining multiple bookies — important business rule
  - Idempotency key is optional in claim flows (unlike create flows) since user may retry after failures
  - Player name derivation: split email at '@' and use prefix — fallback to 'Member' if no email
  - invite version increment on claim helps with sync conflict resolution
---
