# Ralph Progress Log
Started: Mon Mar  2 19:47:05 PST 2026
---

## Codebase Patterns
- Web dashboard uses Alpine.js (`x-data`, `x-show`, `x-cloak`, `x-for`, `x-text`, `@click`) with Supabase JS client
- Routes are hash-based (`#/events`, `#/members/{id}`) parsed in `parseRoute()` — add new routes to the `routes` array
- Sidebar nav links in `app.html` between `<ul class="sidebar-nav">` tags
- Views are `x-show="route === 'xxx'"` sections inside `<main class="main-content">`
- Use existing CSS classes: `.card`, `.card-header`, `.card-title`, `.chip`, `.chip.active`, `.badge`, `.badge-muted/warning/success/danger`, `.data-table`, `.search-input`, `.filter-bar`, `.empty-state`, `.shimmer`
- Events are shared (bookie_id IS NULL) — use `.is('bookie_id', null)` in Supabase queries
- Sport DB values: `americanfootball_nfl`, `basketball_nba`, etc. — use `formatSportName()` for display
- State variables go at the top of `dashboardApp()`, methods below `init()`
- Computed getters use `get` syntax for Alpine reactivity
- Edge functions called via `callEdgeFunction(name, body)` — always include `idempotency_key: crypto.randomUUID()`
- Modals placed outside `<template x-if>` blocks to survive re-renders — at the view section level

---

## 2026-03-02 - US-001
- Implemented Events tab with sport browsing and search
- Files changed: `landing/dashboard/app.html`, `landing/dashboard/dashboard.js`, `landing/dashboard/dashboard.css`
- Added: sidebar nav link with calendar SVG icon, `#/events` route, `loadEvents()` fetching shared events (last 14 days), `filteredEvents` computed getter with search + sport filter + 48h final-event hiding, `groupedFilteredEvents` grouping by sport/league, `eventSportOptions` dynamic sport chips, `formatSportName()`/`formatLeagueName()`/`eventStatusBadgeClass()`/`formatEventTime()` helpers, responsive `.event-row` CSS
- **Learnings for future iterations:**
  - Alpine.js computed getters use `get` keyword — they automatically re-evaluate when reactive data changes
  - Sport filter uses display names (e.g., "Football") not DB keys (e.g., "americanfootball_nfl")
  - Events query uses `.is('bookie_id', null)` for shared events, `.gte('start_time', ...)` for date filtering
  - Event rows link to `#/events/{id}` which will be handled by US-002 (Event Detail)
---

## 2026-03-02 - US-002
- Implemented Event Detail view with odds, exposure breakdown, and pick management
- Files changed: `landing/dashboard/app.html`, `landing/dashboard/dashboard.js`, `landing/dashboard/dashboard.css`
- Added: `#/events/{id}` route in parseRoute(), `selectedEventId`/`eventDetail`/`eventDetailBets`/`eventDetailMarkets`/`isLoadingEventDetail` state, `loadEventDetail()` fetching event + markets (bookie_id IS NULL) + bets (by bookie_id), `eventExposureBreakdown` and `eventTotalExposure` computed getters, hero card with status/matchup/final_score, markets card with h2h/spreads/totals/outright display, exposure breakdown by side, picks table with member links
- **Learnings for future iterations:**
  - Markets are shared (`bookie_id IS NULL`) but bets are bookie-specific — query markets with `.is('bookie_id', null)` and bets with `.eq('bookie_id', bookie.id)`
  - Spread lines: `spread_line` is from the away team's perspective. Home spread = negation of away spread
  - Event detail route must be checked BEFORE the member/pick routes in parseRoute() to avoid `events/xxx` matching other patterns
  - Odds display uses `.odds-mono` CSS class for monospace font
---

## 2026-03-02 - US-003
- Implemented batch grading and event management actions on Event Detail
- Files changed: `landing/dashboard/app.html`, `landing/dashboard/dashboard.js`
- Added: Actions card with Grade All Picks (Win Home/Win Away/Push), Void All Picks, Enter Final Score, Change Status. Batch grade/void modals with progress indicators, final score modal with pre-populated values, change status modal with cancel warning. Computed getters `eventGradableBets` and `eventVoidableBets` for filtering eligible bets. Sequential edge function calls with per-pick progress feedback.
- **Learnings for future iterations:**
  - Batch grading calls `grade_bet` sequentially (not parallel) to respect rate limits — use a for-loop with progress tracking
  - Gradable bets: `['accepted', 'readyToGrade']`. Voidable bets: `['pending', 'accepted', 'readyToGrade']`
  - Final score format in DB: `"away_score-home_score"` string
  - Modals placed outside `<template x-if>` blocks to avoid being destroyed when data changes — they live at the same level as the event-detail section
  - No new CSS needed — reused existing `.btn-grade`, `.btn-grade-win`, `.btn-grade-loss`, `.btn-grade-push`, `.btn-void`, `.modal-overlay`, `.modal`, `.modal-actions`
  - `callEdgeFunction()` is the standard way to call edge functions — returns data with optional `.error`
---

## 2026-03-02 - US-005
- Implemented CSV export for settlement reports
- Files changed: `landing/dashboard/app.html`, `landing/dashboard/dashboard.js`
- Added: Export CSV button (btn-ghost with download SVG icon) inside settlement week selector div, `exportSettlementCSV()` method generating CSV with exact column headers (Member Name, Starting Balance, Bets Won, Bets Lost, Adjustments, Ending Balance, Status), TOTAL summary row, proper CSV field escaping, `booki-settlement-YYYY-MM-DD.csv` filename from week-ending Sunday, Blob download pattern, toast on success, button disabled when loading or empty
- **Learnings for future iterations:**
  - CSV field escaping: wrap in quotes and double internal quotes when field contains commas, quotes, or newlines
  - Settlement week date (`this.settlementWeek`) is the Sunday; use it directly for the filename date
  - The `settlementReports` array has `isPaid` boolean and `endingBalance` for determining Settled/Unpaid status
  - Blob download pattern: create Blob → createObjectURL → createElement('a') → set href+download → click → revokeObjectURL
---

## 2026-03-02 - US-004
- Implemented Settlement view with weekly periods and per-member reports
- Files changed: `landing/dashboard/app.html`, `landing/dashboard/dashboard.js`, `landing/dashboard/dashboard.css`
- Added: sidebar nav link with dollar sign SVG, `#/settlement` route, settlement state variables, `mostRecentSunday()`/`getWeekStart()`/`formatWeekRange()`/`navigateWeek()` helpers, `loadSettlement()` fetching ledger entries and computing per-member settlement reports (starting balance, bets won/lost, adjustments, ending balance), `filteredSettlementReports`/`settlementOwedToYou`/`settlementYouOwe` computed getters, `settlementMarkPaid()` calling `adjust_balance` edge function with `paymentLogged` type, settlement view with week navigator, summary row, filter chips (All/Unsettled/Settled/Needs Attention), data table, Mark Paid confirmation modal, shimmer loading skeleton, empty state
- **Learnings for future iterations:**
  - Settlement calculation splits ledger entries into "before week start" (starting balance) and "within week" (activity), by type
  - Settlement types: `settlement` = grading results (positive = player lost, negative = player won from bookie perspective), `paymentLogged` = settle-ups, `adjustment` = manual adjustments
  - Mark Paid calls `adjust_balance` with `amount: -endingBalance` and `type: 'paymentLogged'` — negating the balance to zero it out
  - Week runs Mon–Sun: `mostRecentSunday()` gets the most recent Sunday, `getWeekStart()` subtracts 6 days for Monday
  - Added `.table-responsive` wrapper for horizontal scrolling on mobile data tables
---
