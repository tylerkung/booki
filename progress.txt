# Ralph Progress Log
Started: Sat Feb 21 17:41:31 PST 2026

## Codebase Patterns
- project.pbxproj has 4 build configuration sections: Debug (project), Release (project), Debug (target), Release (target). Each has its own IPHONEOS_DEPLOYMENT_TARGET and SWIFT_VERSION.
- supabase-swift package uses `upToNextMajorVersion: 2.0.0` — no change needed for iOS 26 migration
- Xcode IDE required for full builds — command-line can't verify typecheck (per CLAUDE.md)
- Tab API: Use `Tab("Title", systemImage: "icon") { content }` (no selection) or `Tab("Title", systemImage: "icon", value: N) { content }` (with selection binding). `.badge()` goes on the Tab.

---

## 2026-02-21 - US-001
- Updated IPHONEOS_DEPLOYMENT_TARGET from 17.0 → 18.0 in all 4 places in project.pbxproj
- Updated SWIFT_VERSION from 5.0 → 6.0 in all 4 places in project.pbxproj
- supabase-swift 2.x constraint compatible — no change needed
- Files changed: Booki.xcodeproj/project.pbxproj
- **Learnings for future iterations:**
  - project.pbxproj has exactly 4 instances each of IPHONEOS_DEPLOYMENT_TARGET and SWIFT_VERSION (2 project-level configs + 2 target-level configs)
  - `replace_all` in Edit tool is efficient for these uniform replacements
  - Can't verify typecheck from command line — must rely on Xcode IDE
---

## 2026-02-21 - US-002
- Wrapped entire body of `configureAppAppearance()` in `if #unavailable(iOS 26) { ... }`
- On iOS 26, tab bar and nav bar get system Liquid Glass translucency (no opaque override)
- On iOS 18-25, existing opaque dark appearance is preserved
- `.tint(Theme.accent)` already on TabView in ContentView.swift handles tab color natively
- Files changed: Booki/BookiApp.swift
- **Learnings for future iterations:**
  - `if #unavailable(iOS 26)` is the correct guard to run code only on older OS versions
  - ContentView.swift line 195 already has `.tint(Theme.accent)` on the TabView — no additional work needed for tab tint
  - UIKit appearance proxies are the main thing to guard; SwiftUI modifiers work across OS versions
---

## 2026-02-21 - US-003
- Wrapped UITabBarItem badge appearance code in ContentView.init() with `if #unavailable(iOS 26) { ... }`
- On iOS 26, badges use system default styling (Liquid Glass compatible)
- On iOS 18-25, badges still use brand teal background with dark text in SpaceGrotesk-Bold
- SwiftUI `.badge()` modifiers on Picks and Members tabs remain unchanged (work independently)
- Files changed: Booki/ContentView.swift
- **Learnings for future iterations:**
  - Badge appearance code (UITabBarItem.appearance().badgeColor, setBadgeTextAttributes) is in ContentView.init(), not in BookiApp
  - SwiftUI `.badge()` modifier works independently of UIKit badge appearance — no need to guard those
  - Same `if #unavailable(iOS 26)` pattern used for both configureAppAppearance() and badge styling
---

## 2026-02-21 - US-004
- Replaced all `.foregroundColor(` with `.foregroundStyle(` across 8 Swift files
- 59 occurrences replaced total: GameCardView (18), GameDetailView (16), CompactGameRow (12), GamesView (3), EventsListView (3), CompactOddsButton (3), PickCardCompact (2), Theme.swift (2)
- Zero occurrences of `.foregroundColor(` remain in Booki source
- Files changed: GameCardView.swift, GameDetailView.swift, CompactGameRow.swift, GamesView.swift, EventsListView.swift, CompactOddsButton.swift, PickCardCompact.swift, Theme.swift
- **Learnings for future iterations:**
  - `.foregroundColor()` and `.foregroundStyle()` are signature-compatible for single Color values — pure find-and-replace
  - `replace_all` in Edit tool handles all occurrences in a file efficiently
  - Note: PickCardCompact.swift uses `.foregroundColor()` inside Text concatenation (`+` operator) — `.foregroundStyle()` works the same way there
---

## 2026-02-21 - US-005
- Replaced all `.cornerRadius(X)` with `.clipShape(RoundedRectangle(cornerRadius: X))` in 5 high-count files
- 30 occurrences replaced total: GradeEventSheet (7: values 16/12/8), BetsListView (7: Theme.cornerRadius/Theme.cornerRadiusSmall), WeeklySettlementView (6: Theme.cornerRadiusSmall/Theme.cornerRadius), PlayerClaimView (6: value 12), UserAgreementView (4: Theme.cornerRadiusSmall/Theme.cornerRadius)
- Files changed: GradeEventSheet.swift, BetsListView.swift, WeeklySettlementView.swift, PlayerClaimView.swift, UserAgreementView.swift
- **Learnings for future iterations:**
  - `.cornerRadius(X)` → `.clipShape(RoundedRectangle(cornerRadius: X))` is a straightforward replacement
  - Files use mix of literal values (8, 12, 16) and Theme constants (Theme.cornerRadius, Theme.cornerRadiusSmall)
  - `replace_all` in Edit tool works per-file per-pattern — need separate calls for each distinct cornerRadius value
  - All 5 files are in Booki/Views/ directory
---

## 2026-02-21 - US-006
- Replaced all remaining `.cornerRadius(X)` with `.clipShape(RoundedRectangle(cornerRadius: X))` across 18 files
- 34 occurrences replaced total: SettlementSnapshotCard (3), PlayerLoginView (3), SignUpView (3), LoginView (3), PlayersListView (2), OnboardingProfileView (2), NumericKeypadView (2), GameDetailView (2), EventDetailView (2), PickDetailHeader (2), AppleSignInButton (2), Theme.swift (2), TonightsExposureCard (1), SyncGamesView (1), GradingView (1), CompactOddsButton (1), CompactGameRow (1), SyncService (1)
- Zero occurrences of `.cornerRadius(` remain in the entire Booki codebase
- Files changed: SettlementSnapshotCard.swift, PlayerLoginView.swift, SignUpView.swift, LoginView.swift, PlayersListView.swift, OnboardingProfileView.swift, NumericKeypadView.swift, GameDetailView.swift, EventDetailView.swift, PickDetailHeader.swift, AppleSignInButton.swift, Theme.swift, TonightsExposureCard.swift, SyncGamesView.swift, GradingView.swift, CompactOddsButton.swift, CompactGameRow.swift, SyncService.swift
- **Learnings for future iterations:**
  - Special case: `LoginView.swift` had `.background(Theme.danger.cornerRadius(...))` — calling `.clipShape()` on a Color (which is a View) works fine
  - The Edit tool requires a full `Read` of the file before editing — partial reads (limit/offset) don't count for some files
  - With US-005 + US-006 combined, all `.cornerRadius()` deprecation warnings are eliminated
---

## 2026-02-21 - US-007
- Migrated TabView from `.tabItem { Label() }` to `Tab(title:systemImage:) { content }` API
- ContentView.swift: 6 bookie tabs migrated (Dashboard, Picks, Members, Events, Grading, Settings)
- PlayerMainView.swift: 3 player tabs migrated with `value:` parameter for selection binding (Games, Track, Account)
- `.badge()` modifiers preserved on Picks and Members tabs
- Zero occurrences of `.tabItem` remain in codebase
- Files changed: Booki/ContentView.swift, Booki/Views/PlayerMainView.swift
- **Learnings for future iterations:**
  - Tab API with selection binding uses `Tab("Title", systemImage: "icon", value: 0) { content }` — the `value:` parameter replaces `.tag()`
  - Tab API without selection binding (bookie mode) uses simpler `Tab("Title", systemImage: "icon") { content }`
  - `.badge()` modifier goes on the `Tab` directly, same as before
  - Content goes inside the Tab closure instead of before `.tabItem`
---

## 2026-02-21 - US-009
- Migrated SyncService from ObservableObject to @Observable macro
- Removed @Published from 4 properties (syncStatus, lastSyncedAt, pendingChangesCount, syncProgressDescription)
- Updated BookiApp.swift: @StateObject → @State, .environmentObject() → .environment()
- Updated 10 views across 9 files: @EnvironmentObject → @Environment(SyncService.self)
- Files changed: SyncService.swift, BookiApp.swift, ContentView.swift, AnalyticsDashboardView.swift, AuthGateView.swift, DashboardView.swift, FetchScoresView.swift, GamesView.swift, PlayersListView.swift (2 sub-views), RefreshOddsView.swift, SyncGamesView.swift
- **Learnings for future iterations:**
  - SyncService is used in 10 views (2 sub-views in PlayersListView: PlayerDetailView and AddPlayerSheet)
  - Same migration pattern as US-008: class declaration, property wrappers, BookiApp injection, and all consumer views
  - `replace_all` in Edit tool efficiently handles files with single occurrences; PlayersListView had 2 occurrences handled in one call
---
