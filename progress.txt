# Ralph Progress Log
Started: Mon Feb 23 00:39:19 PST 2026
---

## 2026-02-23 - US-001
- Created `Booki/Models/Invite.swift` — SwiftData `@Model` with all required fields
- Fields: id (UUID), bookieId (UUID), inviteCode (String), email (String?), createdAt (Date), expiresAt (Date), claimedAt (Date?), claimedByPlayerId (UUID?), needsSync (Bool), version (Int)
- Computed properties: `isExpired` (expiresAt < Date()), `isPending` (claimedAt == nil && !isExpired)
- Explicit `init()` with all parameters and sensible defaults
- Added `Invite.self` to ModelContainer schema in BookiApp.swift
- Added file to Xcode project (pbxproj)
- Files changed: Booki/Models/Invite.swift (new), Booki/BookiApp.swift, Booki.xcodeproj/project.pbxproj
- **Learnings for future iterations:**
  - Xcode project uses pbxproj — new Swift files must be added to 4 sections: PBXBuildFile, PBXFileReference, group children, and Sources build phase
  - Model doesn't conform to Syncable yet (needs lastSyncedAt) — will be added in US-005 when sync support is implemented
  - SwiftData schema change requires deleting app from Simulator
---

## 2026-02-23 - US-002
- Created `supabase/migrations/009_invites.sql` — invites table with full schema
- Table columns: id (uuid PK), bookie_id (uuid FK), invite_code (text UNIQUE), email (text), created_at, expires_at, claimed_at, claimed_by_player_id (uuid FK), version (int)
- RLS: bookies can SELECT and INSERT own invites; UPDATE/DELETE via service role only (for edge functions)
- Indexes: invite_code (fast lookups during claim), bookie_id (bookie's invite list)
- Documented migration in SUPABASE_MIGRATIONS.md
- Files changed: supabase/migrations/009_invites.sql (new), SUPABASE_MIGRATIONS.md
- **Learnings for future iterations:**
  - RLS pattern for bookie-owned tables: `bookie_id IN (SELECT id FROM bookies WHERE auth_user_id = auth.uid())`
  - Edge functions that modify records on behalf of users (like claiming) need service role — don't add UPDATE RLS policies for those
  - Migration numbering is sequential: check last file number before creating new one
---

## 2026-02-23 - US-003
- Created `supabase/functions/create_invite/index.ts` — server-authoritative invite creation
- Validates JWT, confirms caller is a bookie (queries bookies table for auth_user_id)
- Generates unique 8-char invite code (charset: A-Z excluding O/I/L + 2-9 excluding 0/1) with collision retry (up to 5 attempts)
- Sets expiry to 24 hours from creation
- Inserts invite into `invites` table with bookie_id from authenticated user
- Returns `{ success, invite_id, invite_code, invite_url: 'booki://invite/{code}', expires_at }`
- Full idempotency support, CORS, audit event emission
- Updated `_shared/audit.ts` — added `'invite'` to EntityType, `'invite_created'` and `'invite_claimed'` to ActionType
- Files changed: supabase/functions/create_invite/index.ts (new), supabase/functions/_shared/audit.ts
- **Learnings for future iterations:**
  - Edge function pattern: validate JWT → check idempotency → validate caller role → business logic → insert → audit → store idempotency → return
  - `crypto.getRandomValues()` available in Deno for secure random bytes
  - Invite code charset excludes ambiguous chars (0/O/1/I/L) for readability
  - Added `invite_claimed` ActionType proactively for US-004
---

## 2026-02-23 - US-004
- Created `supabase/functions/claim_invite/index.ts` — server-authoritative invite claiming
- Request body: `{ invite_code, auth_user_id, idempotency_key? }`
- Validates invite exists, is not expired (`expires_at > now()`), and is not already claimed (`claimed_at IS NULL`)
- Verifies auth user exists via `admin.getUserById()`
- Checks auth user isn't already linked to any bookie: queries players for `auth_user_id` WHERE `claimed_at IS NOT NULL` — returns 409 `already_in_group` if found
- Creates new Player record: name = email prefix, status = 'active', credit_limit = 10000, claimed_at = now()
- Updates invite record: claimed_at = now(), claimed_by_player_id = new player ID
- Auto-confirms email via `admin.updateUserById(auth_user_id, { email_confirm: true })`
- Returns `{ success, player_id, bookie_id, player_name }`
- Full idempotency support (optional key), CORS, audit event emission
- Uses service role client to bypass RLS (same pattern as claim_player)
- Files changed: supabase/functions/claim_invite/index.ts (new)
- **Learnings for future iterations:**
  - claim_invite differs from claim_player: creates a NEW player record instead of updating an existing one
  - The `already_in_group` check prevents users from joining multiple bookies — important business rule
  - Idempotency key is optional in claim flows (unlike create flows) since user may retry after failures
  - Player name derivation: split email at '@' and use prefix — fallback to 'Member' if no email
  - invite version increment on claim helps with sync conflict resolution
---

## 2026-02-23 - US-005
- Added invite sync to SyncService — download invites from Supabase and upsert into local SwiftData
- Added `InviteRecord` Codable struct to SyncModels.swift for decoding Supabase responses
- Added `invites` case to `SyncableTable` enum with display name
- Added `downloadInvites()` method — fetches invites where `bookie_id` matches current bookie
- Added `upsertInvite()` method — matches on `id`, uses `version` for conflict resolution (invites table has no `updated_at` column)
- Added `Invite.self` to `clearLocalData()` to prevent cross-account data leakage
- Added `invites` to both download and upload ordered tables arrays (upload is a no-op since invites are created server-side via edge function)
- Files changed: Booki/Services/SyncService.swift, Booki/Services/SyncModels.swift
- **Learnings for future iterations:**
  - Invites table has no `updated_at` column — use `version` field for conflict resolution instead of `updatedAt > lastSyncedAt` pattern
  - Invites are server-authoritative (created via `create_invite` edge function) — no client upload needed, just download
  - No pagination needed for invites (unlikely to exceed page limit per bookie)
  - When adding new models to sync: update SyncableTable enum, clearLocalData, downloadAll ordered tables, downloadTable switch, uploadTableChanges switch, uploadPendingChanges ordered tables
---

## 2026-02-23 - US-006
- Restyled PlayersListView: replaced `List { Section { ... } }` with `ScrollView { VStack(spacing: 12) }` layout
- Each member row now uses `Theme.cardBackground` with `Theme.cornerRadiusSmall` rounded corners, full-width card style
- Removed `CollectionFilter` enum, filter toggle, layout toggle icons, and `+` nav bar button from toolbar
- Replaced toolbar with custom inline header: `BookiWordmark` image (leading, height 20) + `SyncStatusIndicator` (trailing) — matching AnalyticsDashboardView toolbar pattern
- Removed `.listRowBackground`, `.scrollContentBackground(.hidden)`, `.navigationTitle("Members")` modifiers (no longer using List)
- Added `@Environment(SyncService.self)` for SyncStatusIndicator
- Kept `AddPlayerSheet` and `AddPlayerInterstitialSheet` structs in file (US-014 will clean those up)
- Files changed: Booki/Views/PlayersListView.swift
- **Learnings for future iterations:**
  - AnalyticsDashboardView toolbar pattern: `ToolbarItem(.topBarLeading)` with `Image("BookiWordmark")` height 20, `ToolbarItem(.topBarTrailing)` with `SyncStatusIndicator`
  - When converting List to ScrollView: remove `.scrollContentBackground(.hidden)`, `.listRowBackground`, use `.background(Theme.background)` directly on ScrollView
  - `NavigationLink` inside ScrollView/VStack needs `.buttonStyle(.plain)` to avoid default highlight styling
  - Unreferenced structs in the same file still compile fine — safe to leave for later cleanup stories
---

## 2026-02-23 - US-007
- Added Pending Invites section to PlayersListView above the members list
- Added `@Query private var invites: [Invite]` and computed `pendingInvites` (where `claimedAt == nil`)
- Created `PendingInviteRow` struct: shows email or "Link invite" label, monospaced invite code, expiration countdown ("Expires in Xh Ym" or "Expired" in Theme.danger)
- Muted visual treatment: `Theme.elevatedBackground` background, `Theme.textSecondary` text
- Context menu delete gesture (`.contextMenu` with destructive delete button) since `.swipeActions` only works in `List`
- "PENDING INVITES" section header with `Theme.caption`, `Theme.textSecondary`, `tracking(1.0)`
- Updated empty state to show "No Members" only when both members AND invites are empty
- Updated Preview model container to include `Invite.self`
- Files changed: Booki/Views/PlayersListView.swift
- **Learnings for future iterations:**
  - `.swipeActions` modifier only works inside `List` — for ScrollView/VStack layouts, use `.contextMenu` with destructive button as alternative
  - Expiration countdown: use `expiresAt.timeIntervalSince(Date())` then divide by 3600 for hours, mod 3600/60 for minutes
  - Pending invites include both active and expired (UI shows "Expired" label), only exclude claimed invites
---

## 2026-02-23 - US-008
- Added "Invite Member" button at the bottom of PlayersListView — full-width, Theme.accent background, dark text, rounded corners
- Created `InviteMemberSheet` with state machine (idle → loading → generated → error)
- "Generate Link" button calls `create_invite` edge function via `EdgeFunctionService.shared.callFunction`
- After generation: displays invite code in large 28pt monospaced font, "Expires in 24 hours" subtitle
- Copy button: copies `booki://invite/{code}` to `UIPasteboard.general.string` with haptic feedback
- Share button: uses SwiftUI `ShareLink` with the invite URL string
- "Done" button to dismiss, loading spinner while in-flight, error state with "Try Again" button
- Created `CreateInviteResponse` Decodable struct with snake_case CodingKeys for edge function response
- Files changed: Booki/Views/PlayersListView.swift
- **Learnings for future iterations:**
  - `EdgeFunctionService.callFunction` is generic — define a Decodable response struct with snake_case CodingKeys for each edge function
  - `create_invite` requires `idempotency_key` in the request body — generate with `UUID().uuidString.lowercased()`
  - SwiftUI `ShareLink` is simpler than wrapping `UIActivityViewController` — use it when sharing a plain string
  - Sheet state machines (enum with associated values) keep view logic clean vs. multiple @State booleans
---

## 2026-02-23 - US-009
- Added "Send via Email" section below Generate Link in InviteMemberSheet
- Email text field with `.keyboardType(.emailAddress)`, `.textContentType(.emailAddress)`
- "Send Invite" button calls `create_invite` edge function with email parameter
- After generation: opens `MFMailComposeViewController` via `MailComposeView` (UIViewControllerRepresentable wrapper)
- Mail pre-filled with: recipient (entered email), subject, body including bookie name and invite link
- If `canSendMail()` is false: copies link to clipboard with haptic + shows fallback alert
- After mail dismiss or fallback: shows "Invite sent!" confirmation state (`emailSent` enum case)
- Created `MailComposeView` struct wrapping `MFMailComposeViewController` with Coordinator pattern
- Added `@Query private var bookies: [Bookie]` for bookie name in email body
- Bookie name fetched via `bookies.first?.name ?? "a group"` in `mailBody(code:)` helper
- Files changed: Booki/Views/PlayersListView.swift
- **Learnings for future iterations:**
  - `MFMailComposeViewController` requires `import MessageUI` — wrap in UIViewControllerRepresentable with Coordinator as delegate
  - `MFMailComposeViewController.canSendMail()` is a static check — always check before presenting, provide clipboard fallback
  - Don't use `let` assignments inside `@ViewBuilder` closures — extract to helper functions instead
  - Email section in idle state uses separate state tracking (`emailInviteCode`, `showingMailCompose`) rather than mixing with the link generation state machine
  - `create_invite` edge function accepts optional `email` param in the request body
---

## 2026-02-23 - US-010
- Registered `booki` URL scheme in Info.plist under `CFBundleURLTypes`
- Added `.onOpenURL` modifier in BookiApp.swift with `handleIncomingURL()` parsing logic
- URL parsing: scheme == "booki", host == "invite", first path component = invite code
- Added `pendingInviteCode` @State in BookiApp, passed as @Binding to AuthGateView
- AuthGateView routes based on auth state: unauthenticated → `.inviteClaim`, player → `.inviteClaim`, bookie → alert
- Added `.inviteClaim` case to AuthViewType enum
- Created minimal `InviteClaimView.swift` stub (US-011 will flesh it out) with `initialCode`, `onNavigateToLogin`, `onClaimComplete` interface
- Added InviteClaimView.swift to Xcode pbxproj
- Files changed: Booki/Info.plist, Booki/BookiApp.swift, Booki/Views/AuthGateView.swift, Booki/Views/InviteClaimView.swift (new), Booki.xcodeproj/project.pbxproj
- **Learnings for future iterations:**
  - URL scheme registration: add `CFBundleURLTypes` → `CFBundleURLSchemes` array in Info.plist
  - `.onOpenURL` should go on root view in WindowGroup, not on AuthGateView — keeps URL handling at app level
  - `url.host` returns "invite" for `booki://invite/CODE`, `url.pathComponents` returns ["/", "CODE"]
  - When creating a view that will be fleshed out later, define the full interface (init params) now so dependent code compiles
  - AuthGateView needed @Binding for pendingInviteCode (not @State) since it's owned by BookiApp
---

## 2026-02-23 - US-011
- Fleshed out `InviteClaimView.swift` from stub to full invite claim landing screen
- State machine with `ViewStep` enum: `.codeEntry`, `.validating`, `.landing`, `.error`, `.newUserSignup`, `.existingUserLogin`
- Manual code entry: 8-char limit, auto-uppercase, filters non-alphanumeric (same pattern as PlayerClaimView)
- On code submit or deep link: validates against Supabase `invites` table with bookie name join
- Valid invite shows: BookiLogo, "You've been invited to join [Bookie Name]'s group", Get Started + Already have an account? CTAs
- Invalid/expired/claimed codes show error state in Theme.danger with Try Again button
- Deep link codes auto-validate on `.onAppear`
- Added `010_invites_public_validate.sql` migration — public SELECT policy on invites table (needed for unauthenticated code validation)
- Updated SUPABASE_MIGRATIONS.md with new migration
- Files changed: Booki/Views/InviteClaimView.swift, supabase/migrations/010_invites_public_validate.sql (new), SUPABASE_MIGRATIONS.md
- **Learnings for future iterations:**
  - RLS on invites only allows bookie SELECT — unauthenticated users validating invite codes need a public SELECT policy
  - PostgREST join syntax: `bookies(name)` works because invites.bookie_id references bookies.id
  - `Theme.bodyFont` is a function, not a property — use `Theme.body` for the static font preset
  - Conditional gradient backgrounds: use `Group { if ... { Theme.buttonGradient } else { LinearGradient(colors: [...]) } }` pattern (not AnyShapeStyle)
  - newUserSignup/existingUserLogin steps are placeholders for US-012/US-013 — keeps CTA buttons functional now
---

## 2026-02-23 - US-012
- Implemented new user signup path in InviteClaimView — "Get Started" CTA now navigates to a full account creation screen
- Signup form: email + password (min 6 chars) + confirm password fields, dark themed with Theme.cardBackground
- On submit: calls `supabase.auth.signUp` → `claim_invite` edge function (via raw URLRequest with anon key, same pattern as PlayerClaimView)
- Race condition prevention: sets `authManager.isClaimingPlayerAccount = true` before signUp to prevent spurious bookie record creation
- After claim success: shows `UserAgreementView` for ToS acceptance
- After agreement: calls `authManager.completePlayerClaimFlow` to sign out invalid session → sign in fresh with confirmed credentials → route to player main view
- Error handling: duplicate email, weak password, network failure shown as inline error text below form
- Loading state on submit button while API calls are in-flight
- Back button returns to landing screen, preserving bookieName and inviteCode
- Added `@Environment(AuthManager.self)` to InviteClaimView
- Updated Preview to include `.environment(AuthManager())`
- Files changed: Booki/Views/InviteClaimView.swift
- **Learnings for future iterations:**
  - `claim_invite` edge function uses anon key (not JWT) because signUp JWT is invalid (email unconfirmed) — same reason as claim_player
  - UUID `.lowercased()` is critical when passing auth_user_id to edge functions (iOS uppercase vs PostgreSQL lowercase)
  - Agreement flow pattern: show UserAgreementView → onAccept calls submitAgreement → then completePlayerClaimFlow (sign out + fresh sign in)
  - Don't need local SwiftData operations — claim_invite creates the player server-side, SyncService will pull it down on next sync
---

## 2026-02-23 - US-013
- Implemented existing user login path in InviteClaimView — "Already have an account?" CTA now navigates to full login flow
- Login form: email + password fields, dark themed matching existing auth screens
- Three-step flow: login → confirmation screen ("Join [Bookie Name]'s group?") → claim_invite call
- On login success: stores session token/userId, navigates to `confirmJoin` step with Confirm/Cancel buttons
- On Confirm: calls `claim_invite` edge function with session access token (valid JWT, unlike signup path which uses anon key)
- If claim returns 409 `already_in_group`: shows error screen with "Unable to Join" header, explanation text, and Back button
- On cancel: signs out, returns to landing screen
- After successful claim: calls `completePlayerClaimFlow` to properly set up auth state
- Added ViewStep cases: `.confirmJoin` for confirmation screen, reused `.existingUserLogin` for `alreadyInGroupError` state
- Added state properties: `loginEmail`, `loginPassword`, `isLoggingIn`, `loginError`, `showAlreadyInGroupError`, `loginSessionAccessToken`, `loginSessionUserId`, `isClaimingInvite`
- Files changed: Booki/Views/InviteClaimView.swift
- **Learnings for future iterations:**
  - Existing user login path uses session access token (valid JWT) for claim_invite — unlike signup path which uses anon key (email unconfirmed)
  - Confirmation screen pattern: login first → show confirmation → call edge function on confirm. This allows Cancel to sign out cleanly before any server-side changes
  - `showAlreadyInGroupError` bool within `.existingUserLogin` step avoids needing another ViewStep case
  - Sign out on 409 error and on Cancel to prevent orphaned sessions
  - `Theme.buttonGradient` type differs from `LinearGradient` — use `Group { if ... }` pattern, not ternary
---
