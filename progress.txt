# Ralph Progress Log
Started: Thu Jan 29 19:02:03 PST 2026
---

## Codebase Patterns
- PlayerClaimView uses Supabase directly for invite code validation (not local SwiftData) because players don't have local data when claiming
- When updating Supabase tables directly, use snake_case for column names (e.g., `auth_user_id`, `claimed_at`)
- Always create a Codable struct with CodingKeys for Supabase responses that use snake_case

---

## 2026-01-29 - US-001
- Implemented direct Supabase query for invite code validation in PlayerClaimView
- Added PlayerClaimRecord struct to decode Supabase response with snake_case CodingKeys
- Updated createAccount() to update players table in Supabase with auth_user_id, claimed_at, updated_at
- Files changed: Booki/Views/PlayerClaimView.swift
- **Learnings for future iterations:**
  - Players claiming accounts don't have local SwiftData records - must query Supabase directly
  - The invite code flow validates against Supabase, creates auth user, then updates players table with auth_user_id
  - ISO8601DateFormatter is needed for date strings sent to Supabase
---
