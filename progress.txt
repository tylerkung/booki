# Ralph Progress Log
Started: Fri Jan 23 14:18:44 PST 2026
---

## Codebase Patterns
- SwiftData models use `@Model` decorator with `@Attribute(.unique)` for id field
- All models should be registered in BookiApp.swift schema array
- New Swift files must be added to project.pbxproj (PBXFileReference, PBXBuildFile, group children, and Sources build phase)
- Xcode project file IDs follow pattern `8A123455XXXX` for file refs and `8A123456XXXX` for build files
- Use `swiftc -parse` for syntax validation but expect SwiftData macro errors (normal)

---

## 2026-01-23 - US-001
- **What was implemented:** AcceptancePolicy SwiftData model for bookie acceptance rules
- **Files changed:**
  - Booki/Models/AcceptancePolicy.swift (new)
  - Booki/BookiApp.swift (added to schema)
  - Booki.xcodeproj/project.pbxproj (added file references)
- **Learnings for future iterations:**
  - SwiftData models follow a consistent pattern: @Model class with @Attribute(.unique) var id: UUID
  - All new models must be added to the Schema array in BookiApp.swift
  - Xcode project file requires 4 edits to add a new source file: PBXBuildFile, PBXFileReference, group children, and Sources build phase
---

## 2026-01-23 - US-002
- **What was implemented:** Added policyViolationReason field to Bet model for storing review reasons
- **Files changed:**
  - Booki/Models/Bet.swift (added policyViolationReason: String? field and updated init)
- **Learnings for future iterations:**
  - Adding optional fields to existing SwiftData models is straightforward - just add the property and update init with default nil
  - No need to modify project.pbxproj or BookiApp.swift schema when modifying existing files
  - SwiftData handles optional String fields without additional configuration
---

## 2026-01-23 - US-003
- **What was implemented:** AcceptancePolicyService for evaluating bets against acceptance policy rules
- **Files changed:**
  - Booki/Services/AcceptancePolicyService.swift (new)
  - Booki.xcodeproj/project.pbxproj (added file references)
- **Learnings for future iterations:**
  - Services in this codebase are implemented as `enum` types with static functions (pure logic, no state)
  - PolicyViolation enum captures all the reasons a bet may fail policy: stakeTooHigh, stakeRequiresReview, newPlayer, parlayNotAllowed, parlayTooManyLegs, eventLocked
  - evaluateBet function returns an array of violations - empty array means bet should be auto-accepted
  - Event lock check also considers event status (.live, .final) regardless of offset
---

## 2026-01-23 - US-004
- **What was implemented:** Integrated policy evaluation into bet submission flow
- **Files changed:**
  - Booki/Services/BetService.swift (added policy parameters and evaluation logic)
  - Booki/Views/BetSlipSheet.swift (added @Query for events and acceptancePolicies, updated submitBets)
- **Learnings for future iterations:**
  - BetService.submitBet now accepts optional policy, isParlay, parlayLegs, playerBetCount, and event parameters
  - When policy is provided: evaluates bet, auto-accepts if no violations, queues for review if violations
  - When .eventLocked violation occurs, returns .failure(.eventLocked) instead of creating a bet
  - BetSlipSheet queries AcceptancePolicy and Events via @Query and passes to BetService
  - Player bet count is calculated from existing bets for new player threshold check
  - Parlay detection uses betMode == .parlay && count > 1
---

## 2026-01-23 - US-005
- **What was implemented:** Acceptance Policy Settings UI for bookie configuration
- **Files changed:**
  - Booki/Views/AcceptancePolicySettingsView.swift (new)
  - Booki/Views/SettingsView.swift (added navigation link under new "Bet Management" section)
  - Booki.xcodeproj/project.pbxproj (added file references)
- **Learnings for future iterations:**
  - Settings views use List with Sections and `.listRowBackground(Theme.cardBackground)` for dark theme
  - Use `.scrollContentBackground(.hidden).background(Theme.background)` for dark list backgrounds
  - SwiftData @Query can be used with computed properties to get first item: `policies.first`
  - Bindings to optional SwiftData properties can use pattern: `Binding(get: { policy?.property ?? default }, set: { getOrCreatePolicy().property = $0 })`
  - ensurePolicyExists() pattern on onAppear creates singleton-like policy record
  - NavigationLink placement in SettingsView follows pattern of section with header and footer
---

## 2026-01-23 - US-006
- **What was implemented:** Display policy violation reasons for pending bets in BetsListView
- **Files changed:**
  - Booki/Views/BetsListView.swift (added policyViolationReason parameter to BetRowView, display violation reason text)
- **Learnings for future iterations:**
  - BetRowView accepts optional policyViolationReason parameter with default nil for backwards compatibility
  - Violation reason displayed with Theme.warning color and "Review: " prefix
  - Only shows for bets with .pending status AND non-nil, non-empty policyViolationReason
  - Use `foregroundStyle(Theme.warning)` for warning-colored text display
---
