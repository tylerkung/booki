## 2026-03-03 - US-010
- US-010: Added sport page with league sub-tabs and futures browsing
- New route '#/player-sport/{sport}' (e.g., #/player-sport/basketball) handled in parseRoute() with sportCategories validation
- State: selectedSportPage, selectedLeagueTab, isLoadingSportPage, sportPageOutrightMarkets, sportCategories data structure
- Computed getters: sportPageCurrentCat, sportPageIsGolf, sportPageLeagueEvents, sportPageFuturesGrouped, sportPageLeagueOutrights
- sportToKey() helper: maps display name (e.g. 'Basketball') back to sport key for navigation
- Games view sport chips now navigate to sport page instead of inline filtering
- Games view group headers are now tappable with chevron icon → navigate to sport page
- Sport page HTML: back breadcrumb, sp-tab-bar with league tabs + Futures tab (non-golf), game rows reusing pg-game-row pattern, outright futures cards
- Golf special case: league tabs render outright markets directly (no separate Futures tab) — sportPageIsGolf flag
- Bet slip bar + modal moved outside all route x-show containers to fix display:none visibility bug; now shared x-show condition on route === 'player-games' || 'player-sport'
- Outright markets use side_a/odds_a columns fetched separately in loadSportPage() (separate from playerMarkets which uses different columns)
- Files changed: landing/dashboard/dashboard.js, landing/dashboard/app.html, landing/dashboard/dashboard.css, prd.json, progress.txt
- **Learnings for future iterations:**
  - position:fixed children are hidden by parent display:none — move shared overlays outside route x-show containers
  - Outright markets have side_a/odds_a columns (not home_odds/away_odds) — need separate DB fetch
  - sportCategories as a state property (not a getter) avoids reactivity issues with Object.entries() calls
  - Golf is isOutrightOnlySport: league tabs show outright markets, no Futures tab
---

## 2026-03-03 - US-014 + US-016 + US-017
- US-014: Added data-label attributes to settlement table (the only table missing them). Picks/members/invite/event-detail/dashboard tables were already complete.
- US-016: Added .member-detail-header CSS class with flex-direction:column + gap at 768px. Changed inline style to class.
- US-017: Added .mobile-page-title span to mobile header showing current route name (Dashboard/Members/Picks/etc.) centered between logo and hamburger.
- Files changed: landing/dashboard/app.html, landing/dashboard/dashboard.css, prd.json, progress.txt
- **Learnings for future iterations:**
  - Only settlement table needed data-label — other tables were already complete
  - mobile-page-title uses x-text with ternary chain to map routes to display names
  - member-detail-header CSS class adds responsive stacking; inline min-width:0 on the name div prevents overflow
---

## 2026-03-03 - US-012 + US-013 + US-015 + US-018 + US-019 + US-020
- US-012: Added playerTimeFilter state + filter logic in filteredPlayerEvents getter (today/tomorrow/thisWeek). Added time filter chips (All/Today/Tomorrow/This Week) below sport chips in Games view. Empty state message updates per filter.
- US-013: Added 480px breakpoint for .toast-container (right/left:12px) and .toast (word-break).
- US-015: Added 480px breakpoint: .pg-odds-btn 60px/10px, hide .pg-odds-line, .pg-team-name 11px, .pg-game-row tighter padding.
- US-018: Added overflow/ellipsis/nowrap to .stat-value globally. Added 768px (24px) and 480px (22px) font-size overrides.
- US-019: At 480px, .filter-bar changes to nowrap + overflow-x:auto + scrollbar hidden.
- US-020: Sidebar uses display:flex+flex-direction:column. .sidebar-bottom changed from position:absolute to margin-top:auto.
- Files changed: landing/dashboard/dashboard.css, landing/dashboard/dashboard.js, landing/dashboard/app.html, prd.json, progress.txt
- **Learnings for future iterations:**
  - sidebar flex-column + sidebar-bottom margin-top:auto is the canonical "stick to bottom" pattern
  - filteredPlayerEvents uses local timezone Date math (new Date with year/month/date constructor)
  - Time filter chips are separate filter-bar row below sport chips — both rows use overflow-x:auto at 480px
---

## 2026-03-03 - US-006 + US-007 + US-008
- US-006: Added isValidUUID() helper; validates UUIDs before setting parameterized routes in parseRoute(). Invalid UUIDs silently redirect to parent route.
- US-007: Wrapped callEdgeFunction fetch in AbortController with 10s timeout. AbortError → 'Request timed out', TypeError → 'Network error', HTTP error without body.error → 'Failed to call {name}'.
- US-008: Added .eq('player_id', this.playerId) to parlay leg query in loadPlayerTicketDetail() — defense-in-depth filter complementing US-002.
- Files changed: landing/dashboard/dashboard.js, prd.json, progress.txt
- **Learnings for future iterations:**
  - isValidUUID helper is now available for other uses in dashboard.js
  - callEdgeFunction() pattern: AbortController cleanup in both success and error paths with clearTimeout(timeoutId)
  - Three small hardening stories handled in one commit for efficiency
---

## 2026-03-03 - US-005
- Fixed parlay gradeResult logic to show 'Lost' immediately when ANY leg has grade_result='loss'
- Fixed in playerTickets getter (list view): `if (legs.some(l => l.grade_result === 'loss'))` sets gradeResult='loss' early
- Fixed in loadPlayerTicketDetail() (detail view): same logic change for the ticket-level gradeResult
- Fixed organizer pick detail hero card: inline conditional checks pickDetailLegs.some(l => l.grade_result === 'loss') for badge/text
- Previously: waiting for legs.every(l => l.grade_result) meant partially-graded losing parlays showed as "Open"
- Files changed: landing/dashboard/dashboard.js, landing/dashboard/app.html, prd.json, progress.txt
- **Learnings for future iterations:**
  - Parlay gradeResult logic is in 2 places: playerTickets getter and loadPlayerTicketDetail() — both need the same fix
  - Organizer pick detail uses pickDetailLegs array (loaded async) so inline conditional works better than a computed
  - The web had no "Partial" badge, just "Open" for partially-graded parlays — the fix makes losing parlays show "Lost" early
---

## 2026-03-03 - US-009
- Removed player-home tab entirely; Games is now the default player landing page
- Removed Home sidebar nav link from app.html
- Removed player-home HTML section (~150 lines) from app.html
- Changed parseRoute() default: 'player-home' → 'player-games'; removed 'player-home' from playerRoutes array
- Changed detectUserRole() redirects: '#/player-home' → '#/player-games' for both 'player' and 'standalone' roles
- Added balance bar to Games view: compact single-row bar above sport filter chips (balance left, available credit right)
- Added Performance card (W-L-P record, win rate, net performance, open picks) at top of Account tab
- Kept loadPlayerHome() as pure data-fetching method; moved call to init() for standalone too
- loadPlayerGames() now calls loadPlayerHome() if bets/ledger not yet loaded (ensures balance bar has data)
- Files changed: landing/dashboard/app.html, landing/dashboard/dashboard.js, prd.json, progress.txt
- **Learnings for future iterations:**
  - loadPlayerHome() is a shared data-loading method used by loadPlayerTrack/loadPlayerAccount/loadPlayerGames — keep it as a pure data fetcher
  - The standalone role wasn't calling loadPlayerHome() during init (only 'player' was) — fixed
  - Balance bar uses x-show="!isLoadingPlayerHome" to avoid flashing $0 while data loads
---

## Codebase Patterns
- dashboard.js playerCreditUtilization and playerAvailableCredit use Math.max(0, balance) not Math.abs — winning players get full credit
- memberExposureMap tracks openStakes per member for accurate credit utilization alerts
- app.html x-show conditions for player views use '|| userRole === "standalone"' for shared player/standalone access
- parseRoute() line 463: playerRoutes array includes 'player-track' so standalone routing works automatically
- loadPlayerTicketDetail() already has .eq('player_id', this.playerId) defense-in-depth filter

# Ralph Progress Log
Started: Tue Mar  3 01:44:11 PST 2026
---

## 2026-03-03 - US-011 (+ confirming US-001, US-002, US-003)
- US-001, US-002, US-003 were already correctly implemented in the parent branch (ralph/web-dashboard-parity). Marked as passes: true.
- US-011: Fixed Track tab visibility for standalone users in app.html:
  - Sidebar Track link: changed x-show from `userRole === 'player'` to `userRole === 'player' || userRole === 'standalone'`
  - Track view container: changed x-show from `route === 'player-track' && userRole === 'player'` to include standalone
- Files changed: landing/dashboard/app.html, prd.json, progress.txt
- **Learnings for future iterations:**
  - US-001/002/003 were pre-implemented — credit calc, player_id security filter, and attention feed openStakes were all done
  - app.html has 3 roles: 'organizer', 'player' (with bookie), 'standalone' (no bookie) — visibility conditions for player features often need standalone added
  - The player-ticket sidebar link (line 2585) still says `userRole === 'player'` — standalone can't see ticket details from track tab (not in US-011 criteria but may need future fix)
---

## 2026-03-03 - US-004
- Added .eq('bookie_id', this.bookie.id) to the main bets query in loadPickDetail() after .eq('id', this.selectedBetId)
- Added .eq('bookie_id', this.bookie.id) to the parlay legs query in loadPickDetail() after .eq('ticket_id', ...)
- Files changed: landing/dashboard/dashboard.js, prd.json, progress.txt
- **Learnings for future iterations:**
  - loadPickDetail() is around line 2670 in dashboard.js — two separate queries: main bet fetch (~2677) and parlay legs fetch (~2710)
  - Other organizer queries like loadMemberDetail already use explicit bookie_id filters — these two were outliers
  - Defense-in-depth pattern: always add explicit ownership filters even when RLS covers it
---

## 2026-03-04 - Launch Stress Tests
- Built 15-suite Node.js stress test suite in `tests/` exercising all critical edge functions against live Supabase
- 142 assertions covering: auth boundaries, bet submission (singles + parlays), idempotency, grading, settlement, overrides, balance adjustments, ledger integrity (hash chain + immutability), concurrent operations (10 parallel bets, 5 parallel settlements), invite flow, accept/decline, credit limits, win limits
- All 15 suites pass green on consecutive runs (~85s), idempotent cleanup verified
- Files changed: tests/runner.js, tests/run.sh, tests/package.json, tests/lib/{client,fixtures,cleanup,assert}.js, tests/suites/01-15
- **Real bug discovered:** `settle_bet_tx` RPC has column mismatch ("INSERT has more target columns than expressions") — documented in suites 06/11
- **Learnings for future iterations:**
  - Supabase auth users can't be deleted (FK constraints) — use find-or-create pattern with password reset
  - `signInWithPassword()` contaminates the Supabase client's internal auth state — must use separate anon clients
  - Node 16 lacks global `fetch`, `crypto.randomUUID()`, `Headers` — requires polyfills (node-fetch, crypto import, supabase-js pinned to 2.43.5)
  - Ledger entries table has `description` not `reason`, `entry_hash` not `hash` — always verify column names against actual schema
  - Ledger immutability trigger blocks cleanup DELETEs — use unique identifiers per test run to avoid stale data collisions
---
