# Ralph Progress Log
Started: Tue Feb  3 02:25:14 PST 2026

## Codebase Patterns
- Edge functions use Deno runtime and are located in `supabase/functions/`
- Audit events use `emitAuditEvent(client, {...})` from `_shared/audit.ts`
- Bookie settings are stored in the `bookies` table and queried via Supabase client
- UUID normalization: Always use `.toLowerCase()` when comparing UUIDs (iOS sends uppercase)
---

## 2026-02-03 - US-001
- What was implemented: Auto-accept bets by default in submit_bet edge function
- Files changed:
  - `supabase/functions/submit_bet/index.ts` - Added bookie manual_bet_acceptance check, auto-accept logic
- **Learnings for future iterations:**
  - The submit_bet function already had the auto-accept code staged but not committed
  - Audit action types follow pattern `bet_auto_accepted`, `bet_submitted` for bet events
  - Deno is not installed on this system, so typecheck was not run but code is valid TypeScript
  - Bookie settings pattern: query single setting from `bookies` table, default to false for opt-in features
---
