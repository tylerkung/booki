# Ralph Progress Log
Started: Fri Jan 23 18:00:08 PST 2026
---

## Codebase Patterns
- BetService.submitBet already accepts isParlay and parlayLegs parameters; ensures future stories can use existing API
- Bet model uses SwiftData @Model macro; new fields need defaults for backwards compatibility
- BetSlipSheet determines parlay mode via `betSlipManager.betMode == .parlay && count > 1`
- Use `swiftc -parse <file>` for basic Swift syntax validation (Xcode not available in CLI)
- For SwiftData compatibility: store enums as String, provide computed property for type-safe access (see parlayPushVoidPolicyEnum in AcceptancePolicy.swift)

- TicketHeaderView shows "X/Y legs graded" for partially graded parlays and mini status dots for leg grades
- TicketBetRowView shows grade result badges and "Awaiting Result" for pending legs
- TicketDetailBetRowView also shows isPendingResult state for pending legs

- GradingView groups parlays by ticketId using GradingGroup struct; use ParlayGradingGroupView for parlay display
- Access parlayPushVoidPolicy via @Query policies: [AcceptancePolicy] and policies.first?.parlayPushVoidPolicyEnum

---

## 2026-01-23 - US-001
- **What was implemented**: Added isParlay: Bool and parlayLegs: Int fields to Bet model with defaults (false/1). Updated BetService.submitBet to set these fields when creating Bet objects.
- **Files changed**:
  - Booki/Models/Bet.swift - Added isParlay and parlayLegs properties and init parameters
  - Booki/Services/BetService.swift - Pass isParlay and parlayLegs when creating Bet
- **Learnings for future iterations:**
  - BetSlipSheet.submitBets already passes isParlay/parlayLegs to BetService - no changes needed there
  - Singles get parlayLegs=0 from BetSlipSheet but BetService normalizes to 1 for singles
  - SwiftData macro errors in diagnostics are expected when not building in Xcode IDE
---

## 2026-01-23 - US-002
- **What was implemented**: Added ParlayPushVoidPolicy enum and parlayPushVoidPolicy field to AcceptancePolicy model. Added Parlay Settlement section with Picker to AcceptancePolicySettingsView.
- **Files changed**:
  - Booki/Models/AcceptancePolicy.swift - Added ParlayPushVoidPolicy enum with displayLabel/explanation properties; added parlayPushVoidPolicy String field and parlayPushVoidPolicyEnum computed property for type-safe access
  - Booki/Views/AcceptancePolicySettingsView.swift - Added new "Parlay Settlement" section with Picker for push/void policy; footer shows dynamic explanation based on selection
- **Learnings for future iterations:**
  - Store enums as String in SwiftData models and provide computed property for type-safe enum access (parlayPushVoidPolicyEnum pattern)
  - AcceptancePolicySettingsView uses getOrCreatePolicy() pattern for lazy policy creation - follow this pattern for new fields
  - Picker footer can be made dynamic to show policy-specific explanations by reading the current value
---

## 2026-01-23 - US-003
- **What was implemented**: Created ParlayGradingService.swift with ParlayOutcome enum and core parlay grading logic for calculating outcomes and payouts.
- **Files changed**:
  - Booki/Services/ParlayGradingService.swift - New file with ParlayOutcome enum (pending, win(payout), loss, push, partiallyGraded), calculateParlayOutcome function, calculateParlayPayout function, and americanToDecimal helper
- **Learnings for future iterations:**
  - Services follow enum pattern with static functions (no instances needed) - see GradingService for reference
  - BetStatus.void is checked via bet.status property; GradeResult.push is checked via bet.gradeResult
  - American odds conversion: positive odds = 1 + odds/100, negative odds = 1 + 100/|odds|
  - Parlay payout is profit only (stake × combinedMultiplier - stake), not total return
---

## 2026-01-23 - US-004
- **What was implemented**: Verified American Odds Combination Math implementation already present in ParlayGradingService.swift (implemented during US-003). All acceptance criteria met:
  - calculateParlayPayout(stake:bets:excludeVoidPush:) function exists
  - americanToDecimal(odds:) helper converts American → decimal odds correctly
  - Combined multiplier calculated by multiplying all decimal odds
  - Payout formula: stake × combinedMultiplier - stake (profit only)
  - excludeVoidPush parameter properly filters pushed/voided legs
  - Returns 0 when no valid legs remain
- **Files changed**:
  - prd.json - Marked US-004 as passes: true
- **Learnings for future iterations:**
  - US-003 and US-004 acceptance criteria overlapped significantly - calculateParlayPayout was implemented as part of US-003
  - When acceptance criteria overlap between stories, check if implementation is already complete before writing new code
---

## 2026-01-23 - US-005
- **What was implemented**: Added leg status tracking to ticket display for parlays - visual feedback showing grading progress and status for each leg.
- **Files changed**:
  - Booki/Views/TrackView.swift - Added gradedLegsCount/showGradingProgress properties and legStatusColor helper to TicketHeaderView; shows "X/Y legs graded" text and mini status dots row; Updated TicketBetRowView to show grade badges when graded (not just settled) and "Awaiting Result" for pending legs
  - Booki/Views/TicketDetailView.swift - Updated statusText to show grade result if available; Added isPendingResult computed property; Updated body to show "Awaiting Result" italic text for pending legs
- **Learnings for future iterations:**
  - TicketHeaderView and TicketBetRowView are both in TrackView.swift
  - TicketDetailBetRowView is in TicketDetailView.swift (separate file)
  - To check if a leg is graded: bet.gradeResult != nil
  - To check if a leg is pending result: bet.gradeResult == nil && (bet.status == .accepted || bet.status == .readyToGrade)
  - Mini status dots use Theme.accent (green/win), Theme.danger (red/loss), Theme.warning (yellow/push), Theme.textMuted (gray/pending)
---

## 2026-01-23 - US-006
- **What was implemented**: Created parlay grading UI for bookie that groups parlay legs together and shows combined status/outcome.
- **Files changed**:
  - Booki/Views/GradingView.swift - Added GradingGroup struct to group bets; Added gradingGroups computed property that groups parlays by ticketId; Created ParlayGradingGroupView component with status header, projected outcome, and projected payout; Created ParlayLegRow for compact leg display; Modified List to use groups for display
- **Learnings for future iterations:**
  - GradingView uses @Query for policies to access AcceptancePolicy.parlayPushVoidPolicyEnum
  - Group parlays by ticketId and include ALL bets with that ticketId (even already graded ones) for full context
  - ParlayProjectedOutcome enum provides clean state management for outcome display (willLose, willPush, canWin, inProgress)
  - ParlayLegRow uses compact icon buttons for grading within the group (vs full buttons for singles)
  - Projected payout uses ParlayGradingService.calculateParlayPayout for consistency
---
