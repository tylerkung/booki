# Ralph Progress Log
Started: Thu Feb 26 14:57:41 PST 2026
---

## Codebase Patterns
- Dashboard SPA uses Alpine.js with hash-based routing in `landing/dashboard/dashboard.js`
- Routes parsed via `parseRoute()` on `hashchange` events — use regex for parameterized routes
- State variables (e.g. `selectedPlayerId`) live in the Alpine data object returned by `dashboardApp()`
- Edge functions called via `callEdgeFunction(name, body)` helper with auth headers
- Player data cached in `playerMap` (keyed by id) for O(1) lookups
---

## 2026-02-26 - US-001
- Implemented parameterized hash routing in dashboard.js
- Added `selectedPlayerId` and `selectedBetId` reactive properties
- `parseRoute()` now uses regex to detect `#/members/<id>` and `#/picks/<id>` patterns
- Added `settings` to supported routes, `member-detail` and `pick-detail` as parameterized routes
- Existing routes unchanged, back navigation works via existing `hashchange` listener
- Files changed: `landing/dashboard/dashboard.js`
- **Learnings for future iterations:**
  - The dashboard.js is a single Alpine.js data object — all state goes in `dashboardApp()` return object
  - Routes use simple hash matching — parameterized routes need regex or string splitting
  - Query params stripped before route matching (`hash.replace(/\?.*$/, '')`)
---
