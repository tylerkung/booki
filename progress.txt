# Ralph Progress Log
Started: Mon Feb 23 17:56:56 PST 2026

## Codebase Patterns
- Sport string values from OddsAPIMapper: "Basketball", "Football", "Baseball", "Hockey", "Soccer" (capitalized)
- League string values: "NBA", "NCAAB", "WNBA", "NFL", "NCAAF", "MLB", "NHL", "EPL", "MLS" (uppercased)
- Xcode pbxproj requires entries in 4 places: PBXBuildFile, PBXFileReference, group children, Sources build phase
- `xcodebuild` not available — Xcode IDE required for full builds; `swiftc -parse` works for syntax checking standalone files
- Event.sport and Event.league are plain String properties, not enums

---

## 2026-02-23 - US-001
- Created `Booki/Models/SportCategory.swift` with `SportCategory` enum (basketball, football, baseball, hockey, soccer)
- `LeagueInfo` struct with id, displayName, and `matchesEvent` closure checking Event.sport + Event.league
- Computed properties: `displayName`, `iconName` (SF Symbols), `leagues` (per-sport league lists)
- Static `fromEvent(_:)` matches Event.sport to SportCategory.displayName
- Static `popular` returns [basketball, football, baseball, hockey] for Search tab
- Added file to Xcode pbxproj (PBXBuildFile, PBXFileReference, Models group, Sources build phase)
- Files changed: Booki/Models/SportCategory.swift (new), Booki.xcodeproj/project.pbxproj
- **Learnings for future iterations:**
  - Sport matching uses `category.displayName == event.sport` since Event.sport stores capitalized sport names
  - LeagueInfo closures match on both sport AND league to avoid cross-sport collisions
  - Keep existing `sportIconName()` in GamesView for now — US-007 will refactor to use SportCategory
---

## 2026-02-23 - US-002
- Created `Booki/Views/SportPageView.swift` with league sub-tabs, filtered games, and Futures placeholder
- Init takes `SportCategory`, optional `isViewOnly: Bool` (default false), optional `Player?` (for bet slip + navigation)
- League sub-tabs rendered as horizontal scroll capsule buttons (same style as GamesView sport tabs)
- Events filtered per selected league using `LeagueInfo.matchesEvent` closure
- Events grouped by date with sticky section headers (Today/Tomorrow/formatted date) + SPREAD/MONEY/TOTAL column headers
- Player mode: bettable events only (scheduled, future, not locked), bet slip selection via BetSlipManager.shared, card tap navigates to GameDetailView
- Bookie mode (isViewOnly=true): shows all non-canceled events, muted odds, no tap/selection
- Futures tab shows centered placeholder (icon + title + subtitle) — satisfies US-003 acceptance criteria
- Empty league state: "No games available" centered message
- Added file to Xcode pbxproj (PBXBuildFile, PBXFileReference, Views group, Sources build phase)
- Files changed: Booki/Views/SportPageView.swift (new), Booki.xcodeproj/project.pbxproj
- **Learnings for future iterations:**
  - SportPageView needs `player` param for GameDetailView navigation — passed from GamesView/SearchView parent
  - Futures placeholder is built into SportPageView (not a separate view), selected via `selectedLeagueId == "futures"`
  - Date grouping preserves insertion order via `dateOrder` array to keep chronological sort
  - Reuse same CompactGameRow + BetSlipManager patterns from GamesView for consistency
---

## 2026-02-23 - US-003
- Futures placeholder already implemented within SportPageView (US-002)
- When "Futures" tab selected: centered VStack with chart.line.uptrend.xyaxis icon (48pt, Theme.textMuted), "Futures Coming Soon" (Theme.headline, Theme.textPrimary), subtitle (Theme.body, Theme.textSecondary)
- No additional files changed — covered by US-002 commit
---

## 2026-02-23 - US-004
- Added "POPULAR SPORTS" section to SearchView when search text is empty
- Replaced generic magnifying glass empty state with scrollable list of SportCategory.popular
- Each row: sport icon (Theme.accent) + displayName (Theme.textPrimary) + chevron.right, on Theme.cardBackground rounded card
- Section header: "POPULAR SPORTS" with Theme.caption, 1.0 tracking, Theme.textSecondary
- NavigationLink(value: SportCategory) navigates to SportPageView(category:player:)
- Added `.navigationDestination(for: SportCategory.self)` on body's outer VStack
- Popular Sports disappears when user types (searchText.isEmpty branch)
- Files changed: Booki/Views/SearchView.swift
- **Learnings for future iterations:**
  - SportCategory is a String raw-value enum so it's automatically Hashable — works with NavigationLink(value:)
  - Place `.navigationDestination` modifiers on the body-level VStack, not inside sub-views, for reliable navigation
  - SearchView already has `player` property, so passing it to SportPageView is straightforward
---

## 2026-02-23 - US-005
- Made sport+league sticky headers in GamesView tappable with NavigationLink to SportPageView
- Added `trailingNavigation: SportCategory?` parameter to `stickyHeader()` function
- When a SportCategory match exists, header shows chevron.right and navigates to SportPageView on tap
- When no match (e.g. unknown sport), header remains static (no chevron, no navigation)
- Added `.navigationDestination(for: SportCategory.self)` to GamesView body
- Favorites header unaffected (uses default nil parameter)
- Files changed: Booki/Views/GamesView.swift
- **Learnings for future iterations:**
  - `stickyHeader` is a generic function — adding optional parameters with defaults preserves backward compatibility
  - `SportCategory.allCases.first { $0.displayName == sport }` is the pattern for matching sport strings to categories
  - NavigationLink inside a sticky header works well — use `.buttonStyle(.plain)` to prevent default button styling
---

## 2026-02-23 - US-006
- Made sport+league sticky headers in EventsListView tappable with NavigationLink to SportPageView (isViewOnly: true)
- Added `trailingNavigation: SportCategory?` parameter to `stickyHeader()` function (same pattern as GamesView)
- Added `.navigationDestination(for: SportCategory.self)` navigating to SportPageView with `isViewOnly: true`
- Sport category lookup via `SportCategory.allCases.first { $0.displayName == sport }`
- Headers without a matching SportCategory remain non-tappable (no chevron)
- Files changed: Booki/Views/EventsListView.swift
- **Learnings for future iterations:**
  - EventsListView stickyHeader now matches GamesView pattern exactly — both accept optional `trailingNavigation`
  - Bookie views pass `isViewOnly: true` to SportPageView (muted odds, no bet slip interaction)
  - The `let sportCategory = ...` declaration inside ForEach body works fine for computed values used in nested closures
---

## 2026-02-23 - US-007
- Replaced `sportIconName(for:)` calls in GamesView and EventsListView with `SportCategory.iconName(for:)`
- Added `static func iconName(for sport: String) -> String` to SportCategory enum (returns iconName or "sportscourt" fallback)
- Removed the standalone free function `sportIconName(for:)` from GamesView.swift (26 lines)
- SportTabButton continues to work unchanged — it accepts `String?` for iconName, now receives non-optional `String`
- Files changed: Booki/Models/SportCategory.swift, Booki/Views/GamesView.swift, Booki/Views/EventsListView.swift
- **Learnings for future iterations:**
  - Sport icon resolution is now centralized in `SportCategory.iconName(for:)` — use this for any future sport-to-icon mapping
  - The old `sportIconName` function supported extra sports (MMA, Boxing, Tennis, Golf) not in SportCategory — if those sports are added later, add cases to SportCategory enum
  - Refactors that remove free functions should verify no other files reference them via `Grep` before committing
---

## 2026-02-23 - Sport Page Polish (post-Ralph)
- **League tab restyling**: Replaced capsule buttons with text-only + accent underline bar, all-caps Space Grotesk (13pt), `.tracking(1.2)`, reduced padding so underline sits on divider
- **Search results tappable headers**: Sport+league headers in SearchView search results now have NavigationLink to SportPageView (chevron.right indicator), matching GamesView pattern
- **Date section headers**: SportPageView `eventsByDate` now always uses actual dates (e.g. "Monday, Feb 24") instead of "Today"/"Tomorrow" — individual game rows still show relative time
- **Sticky header alignment fix**: SportPageView `dateStickyHeader` HStack changed from default spacing to `spacing: 4` to match CompactGameRow odds button spacing
- **Team abbreviation fix**: Reordered `TeamAbbreviations.abbreviation(for:)` to check pro mascot map before college prefix-strip — fixes "Oklahoma City Thunder" → "OKC" instead of "OU" (Oklahoma University)
- Files changed: Booki/Views/SportPageView.swift, Booki/Views/SearchView.swift, Booki/Services/TeamAbbreviations.swift
- **Learnings for future iterations:**
  - TeamAbbreviations lookup order matters: pro mascot must be checked before college prefix-strip to avoid "Oklahoma" matching college map
  - Sticky header HStack spacing must match CompactGameRow's `HStack(spacing: 4)` for column alignment
  - SportPageView league tabs use VStack with RoundedRectangle underline indicator — not capsules
---
